# CONTEXT.md — стан системи AiM Skill

> Канонічний документ стану. Читай його на початку кожної сесії. Оновлюй після кожної завершеної задачі.
> Останнє оновлення: 2026-07-29.

---

## 1. Що це за система

**AiM Skill** — україномовна система **управлінського обліку** для **ТОВ «ЕЙМ СКІЛ»** (ЄДРПОУ 45505924). Переписана з нуля за ТЗ v2 (червень 2026).

**Три напрямки бізнесу:**
1. **Торгівля** комп'ютерною технікою — закупівля у дистрибюторів, перепродаж клієнтам.
2. **Послуги** — разові: рахунок → оплата → виконання → акт.
3. **Агентська винагорода** — посередництво між клієнтом і партнером за комісію.

**Головний принцип:** центр системи — `bank_transactions` (реальні гроші). Усе прив'язується до них. **Борги рахуються автоматично** (сума документів − сума прив'язаних транзакцій), ніколи не вводяться вручну.

---

## 2. Технологічний стек

| Шар | Технологія |
|---|---|
| Фронтенд | React 18 + Vite 5, react-router (HashRouter) |
| Бекенд | Supabase — Postgres, Auth, Storage (приватний бакет `documents`) |
| AI / OCR | Anthropic Claude Sonnet 4.6 через проксі `/api/ai` |
| Графіки / експорт | Recharts, SheetJS/xlsx |
| Стилі | Без CSS-фреймворку — JS-шаблон у `src/lib/styles.js`, іконки Tabler з CDN |
| Деплой | Vercel, авто-деплой з гілки `main` |

**Env:** `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_ANTHROPIC_KEY`.
**Supabase:** проєкт `ivhfwdojjaflvdbdmttf` (eu-central-1). DDL виконується ВРУЧНУ в Dashboard → SQL Editor (PostgREST не виконує DDL).

---

## 3. Структура папок

```
src/
  lib/
    supabase.js        — клієнт
    auth.jsx           — AuthProvider, useUser, RequireAuth
    styles.js          — усі стилі (css, mobileCss)
    fmt.js             — форматування чисел
    ai.js              — OCR через Claude + callClaude() (з авто-повтором)
    statements.js      — парсинг виписок ПУМБ/Mono/Claude
    autoClassify.js    — авто-класифікація транзакцій (стаття/напрям)
    contractorMatch.js — авто-матч контрагента по ЄДРПОУ/назві
    accounts.js        — getAccountBalances() (єдиний розрахунок залишків)
    debts.js           — баланси контрагента (дебіторка/кредиторка)
    pl.js              — P&L, Aging, Dashboard
    orders.js          — статус-машини 3 циклів замовлень
    stockService.js    — FIFO-собівартість, рухи, збірки
    articles.js        — статті P&L (кеш, групування, pl_level)
    contractors.js     — normalizeName, синхронізація, дедуп
    vkursi.js          — дані контрагента за ЄДРПОУ (+ fallback ЄДР)
    companyConfig.js   — реквізити компанії
    docgen/            — генерація документів (pdfmake)
  components/          — Auth, ErrorBoundary, Layout, Kanban, DocGenModal, ui/ContractorSelect
  pages/              — Login, Orders, OrderCard, Contractors, ContractorCard,
                        BankCash, Warehouse, Documents, Analytics, Settings
  App.jsx             — роутер (lazy-load сторінок), AuthProvider
migrations/           — SQL міграції (запускати в Supabase Dashboard) + validate.mjs
api/                  — ai.js (проксі Claude), vkursi.js, edr.js (Vercel serverless)
```

Навігація: Замовлення · Задачі · Контрагенти · Банк/Каса · Склад · Прайси · Документи · Пошта · Аналітика · Закриття періоду · Налаштування. **Розділ «Бюджет» видалено 2026-08-18** (застарілий/непрацюючий — буде переписаний за новим ТЗ; див. запис нижче).

---

## 4. Поточний стан — LIVE в продакшні

Усі **8 фаз ТЗ v2 завершені**, система розгорнута на Vercel (https://aim-skill.vercel.app). Дані реальні (476 транзакцій, 102 контрагенти, 298 товарів, 159 документів).

| Фаза | Стан | Що працює |
|---|---|---|
| 1. БД | ✅ | Additive-схема: 22 таблиці, в'юхи `product_stock`/`contractor_balances`, RLS |
| 2. Фундамент | ✅ | Router, auth-контекст, 8-розділова навігація |
| 3. Контрагенти | ✅ | Список (пошук/фільтр), картка 6 вкладок, авто-борги, Vkursi |
| 4. Замовлення | ✅ | Реєстр (таблиця+Kanban), 3 статус-машини, картка, КП, товари+маржа, авто-субзамовлення, архів/видалення, API для бота |
| 5. Банк/Документи | ✅ | Імпорт ПУМБ/Mono, валідація→P&L, перекази, OCR, генерація, прив'язка tx↔doc |
| 6. Склад | ✅ | Залишки, рухи, FIFO-збірки, Документи→Склад |
| 7. Аналітика | ✅ | P&L Факт/План/Порівняння, Aging, Dashboard, прогноз. **Факт = матриця по періодах: рік→колонки місяців, місяць→колонки днів** (`computePLBreakdown`, sticky 1-а колонка). Розділ «Бюджет» видалено 2026-08-18 (переписується за новим ТЗ) |
| 8. Налаштування | ✅ | Реквізити, статті P&L, рахунки, користувачі |

**Post-launch покращення (зроблено):**
- Рахунки показують **реальний залишок** (усі неігноровані транзакції + початковий залишок), а не лише рух. Картка: Залишок + Надходження + Витрати.
- **Авто-матч контрагента** по ЄДРПОУ/назві при імпорті, авто-класифікації та відкритті непідтверджених.
- **Кнопка «Розпізнати»** для існуючих документів (OCR зі Storage → метадані).
- **Текст не обрізається** ніде (перенос; вузькі місця — ellipsis+title).
- **Авто-повтор OCR/виписок** на перевантаженні Claude API (529/429).
- **Редагування назви файлу** у формі OCR/розпізнавання (оновлює `documents.file_name`).
- **Номер документа** (`documents.doc_number`) — поле у формі OCR (префіл з OCR) + колонка «№» у реєстрі.
- **Фікс:** додано UPDATE-RLS політику на `documents` (її не було → «Розпізнати» тихо не зберігалось). Save кидає помилку, якщо UPDATE зачепив 0 рядків.
- **Редагування + видалення** документа в екрані перегляду (Видалити — для ролей admin/accountant; перевіряє, що рядок справді видалено).
- **Ручна прив'язка документа до руху складу:** кнопка 🔗 біля рухів без документа (Рухи + картка товару) → DocPickerModal з **рекомендацією за датою** (документи з датою близькою до дати руху, бейдж «рекомендовано · ±Nдн», сортування за близькістю) + пошук за назвою/номером.
- **Backfill рухів складу:** 190 рухів source='document' без document_id прив'язано до документів через bank_transaction (транзакція з 1 документом). Для решти 34 показується читабельна мітка джерела (з документа/вручну/збірка) замість сирого 'document'.
- **Склад → документ-джерело:** у вкладці «Рухи» і в картці товару рух показує прив'язаний документ (мітка типу) і кліком відкриває DocModal (138 рухів мають document_id).
- **Часткове покриття tx↔doc:** одна накладна на кілька платежів — прив'язка враховує залишок документа з усіх транзакцій (min(непокрито tx, залишок doc)); у вікні показано «залишок».
- **Дедуплікація документів:** видалено 20 повних дублів (контрагент+тип+номер+сума; 159→139), зв'язки переносяться на копію що лишається. Лишилось 4 групи з однаковим номером але різною сумою — на ручний огляд (ООН/ПРООН).
- **Сортування таблиць** (`components/Sort.jsx`: useSort + SortTh) — клік по заголовку. Застосовано: Документи, Контрагенти, Замовлення, Склад (Залишки+Рухи), Банк/транзакції. **Пошук у транзакціях** за контрагентом/ЄДРПОУ/описом (server-side ilike).
- **Прив'язаний документ у транзакціях:** колонка «Документ» у таблиці й секція в TxModal показують прив'язані документи (або «не прив'язано»); клік відкриває DocModal.
- **Банк: транзакції у табличному вигляді** (як Документи) — колонки Дата/Контрагент/Сума/Напрям/Стаття/статус, рядок клікабельний → модалка `TxModal` (класифікація, підтвердження, ігнор, прив'язка документів). Замість inline-рядків.
- **Сума документа = з ПДВ (gross).** `documents.amount` тепер повна сума (totalAmount), `vat_amount` — у т.ч. ПДВ. Раніше було net → борги занижені, матчинг по сумі не спрацьовував для ПДВ-документів. Backfill 143 документів. Авто-лінк зріс 14→85.
- **Авто-прив'язка документ↔транзакція** (`lib/docMatch.js`): матч за контрагентом + сумою (±0.5%) + датою документа (±15 днів) — усі три разом. У вікні прив'язки кандидати ранжовані з бейджами «сума ✓/дата ±Nдн»; кнопка «Авто-прив'язка» масово лінкує єдині впевнені збіги (рахунки на оплату — нижчий пріоритет, лінк радше на накладну/акт).
- **Захист від подвоєння боргу:** рахунок на оплату / замовлення / договір НЕ створюють борг (лише накладні та акти). Рахунок+накладна на одну угоду = один борг. `countsAsDebt()` у debts.js/pl.js.
- **Дата документа** (`documents.doc_date`, міграція 005) — окреме поле (раніше показувався created_at, дата з форми не зберігалась). Реєстр/вкладки показують doc_date.
- **Спільна модалка документа** (`components/DocModal.jsx`) — переюзана в Документах, картці контрагента і картці замовлення. У вкладках «Документи» тип показується мітками (не сирим ключем), клік по рядку відкриває перегляд з прев'ю/редагуванням/видаленням.
- **Прев'ю документа** у формі OCR/розпізнавання (двоколонкова: файл зліва, поля справа). Клік по рядку реєстру → той самий екран у режимі перегляду (прев'ю + збережені поля, без авто-OCR; кнопки «Розпізнати»/«Зберегти»).
- **Інтеграція корпоративної пошти (Hostinger IMAP/SMTP)** — серверні ендпоінти в `api/` (патерн `api/ai.js`), залежності `imapflow`/`nodemailer`/`mailparser` (лише серверні). **Концепція (фінальна):** окремий розділ **«Пошта»** — вхідні листи НЕ створюють документи автоматично; заявка створюється з листа окремою дією.
  - **Розділ «Пошта» (`pages/Mail.jsx`, маршрут `/mail`, 9-й розділ навігації):** список усіх листів (вхідні/вихідні, незалежно від вкладень), перегляд листа (тіло в sandbox-iframe, вкладення через signed URL), кнопка **«Створити заявку»**.
  - **Синхронізація (`api/mail-sync.js`):** INBOX (`in`) + Sent (`out`, папка шукається за specialUse/назвою) за **останні 10 днів**, вкладення → Storage (`email/<msgid>/`), рядки `emails`. **Документів не створює, листи прочитаними НЕ помічає.** Дедуплікація за Message-ID. Батч `MAX_NEW=40`/запуск (truncated→натиснути ще). Тригери: кнопка «Синхронізувати», Cron (раз/добу), пінгувач (`CRON_SECRET`).
  - **Заявка з листа (`api/order-from-email.js`):** AI (`extractOrderFromEmail` у `_lib.js`) читає текст листа + вкладення → визначає тип (trade/service/agent), контрагента, суму, позиції, опис → створює `orders` (status `new`, `order_number`=padded count+1); контрагента матчить або **створює** (is_client + contact email); вкладення → `documents` (`source='email'`, OCR до 3 шт) + `order_documents`; `emails.order_id` = нова заявка.
  - **Вихідні (`api/mail-send.js`):** SMTP `nodemailer`, надсилання документа клієнту з вкладенням; кнопка «Надіслати email» у `DocModal` (`components/MailSendModal.jsx`, префіл з `contractor_contacts.email`); пише в `mail_log` + `emails` (direction out, видно в «Пошті»). **Надсилання лишається з розділу Документи** (не з «Пошти»).
  - Авторизація: браузер → Supabase access_token (Bearer, `verifyUser`); cron → `CRON_SECRET`. Серверні операції — service-role (`getAdmin`).
- **Замовлення: Kanban-дошка** (`components/Kanban.jsx`) замість діаграми Ганта. Колонки = статуси: фільтр за типом (trade/service/agent) показує повний pipeline цього типу (з порожніми колонками), змішаний режим — лише наявні статуси в канонічному порядку (`STATUS_ORDER`/`labelForStatus` у `lib/orders.js`). Картка: № + тип + клієнт + сума + наступна дія, підсвітка прострочених. Клік відкриває замовлення (без drag-and-drop — статус рухається кнопкою «Наступна дія»). `Gantt.jsx` видалено.
- **Замовлення: архівування + безпечне видалення** (керування з картки замовлення). Архів (`orders.archived_at`, міграція 008) — відновлюваний, архівні приховані з реєстру й не рахуються в KPI, фільтр-чип «Архів» для перегляду. Видалення — двокрокове підтвердження; **блокується**, якщо до замовлення прив'язані документи (пропонує архів), інакше БД каскадом прибирає КП/субзамовлення, а документи/рухи/листи відв'язуються (FK `SET NULL`).
- **Прайси постачальників (розділ «Прайси», 10-й, `pages/PriceLists.jsx`, маршрут `/prices`)** — довідковий шар, **ОКРЕМО від складу** (товари на склад — лише по прихідних накладних; ці таблиці не чіпають stock_movements/products). Таблиці `supplier_price_lists` (імпорт + збережений `column_map` на постачальника) та `supplier_prices` (рядки: sku, name, brand, category, unit, `price`=закупівля, `retail_price`=продаж, `in_stock` як текст), міграція 010. **Імпорт Excel** (`lib/priceLists.js`, SheetJS): вибір постачальника + .xlsx → авто-мапінг колонок за заголовками (редагований) → превʼю → імпорт чанками по 1000; повторний імпорт того ж постачальника **заміщує** старі ціни. **Валюта + курс + ПДВ (міграція 011):** при імпорті задаються Курс USD→UAH і Ставка ПДВ % (одна на файл); валюта ціни закупівлі — режим (вся грн / вся USD / за колонкою + правило). Собівартість грн = ціна × (валюта USD ? курс : 1); зберігається оригінал (`price_original`+`currency`) і обчислена `price` (грн) + `vat_rate`; курс/ПДВ фіксуються на імпорті. **Сирий прайс ERC (16 779 рядків):** Дилер→закупівля, **DDP→валюта (1=грн, 0=USD)** — авто за заголовком DDP; «Монітор» до валюти не стосується. Роздріб завжди грн. **Дод. поля (міграція 012):** `uktzed` (код УКТЗД/ТН ЗЕД), `warranty`, `warranty_term` — мапляться з файлу. **Одиниця виміру** — або колонка, або значення за замовчуванням («шт», застосовується коли колонки нема/клітинка порожня). Авто-мапінг ERC розділяє Код→sku і ТН ЗЕД→uktzed, Гарантія→warranty, Терм. Гар.→warranty_term. **Авто-визначення рядка заголовків** (`guessHeaderRow`) — підтримує файли з шапкою зверху (ERC=рядок 1, MTI=рядок 4). Підказки налаштовані під ERC і **MTI** (Опис→назва, Код товара постачальника→sku, Ціна продажу грн→закупівля, РРЦ грн→роздріб, На складі→наявність, Код УКТЗЕД→uktzed; усе грн). Мапінг редагований і зберігається на постачальника. **Фікс заниженого `!ref`:** MTI-експорт пише `!ref=A1:Z4`, хоча даних 5190 рядків — `parsePriceFile` перераховує діапазон за фактичними клітинками (інакше SheetJS обрізає). MTI перевірено: усе грн, ціни парсяться (пробіл-роздільник тисяч), наявність текстом («10 і більше»). **Пошук/порівняння** по всіх прайсах за назвою/артикулом, список доступний без пошуку (browse), фільтр по постачальнику, пагінація («Показати ще», по 100), сортування за назвою; клік по рядку → картка позиції з усіма полями + порівняння за артикулом (усі постачальники з цим SKU, найдешевше зверху); ціни 0 («під замовлення») показуються як «за запитом» (223 таких у ERC). **Фаза 2 (готово):** у вкладці «Товари» замовлення кнопка **«З прайсу»** → `components/ui/PricePickerModal.jsx` (пошук по всіх прайсах) → клік додає позицію в `order_items` з назвою/одиницею і ціною = роздріб (редагована); на збереженні `resolveProduct` створює/знаходить товар у довіднику. **Маржа (міграція 013):** `order_items.cost_price` — ціна закупівлі в рядку (з прайсу = `price`, з довідника = `buy_price`, редагована); у вкладці «Товари» показується маржа per-рядок (грн + %) і підсумкова маржа. Пошук прайсів — **за словами** (кожне слово в назві/артикулі, AND), стійко до пробілів/апострофа/порядку (та сама `queryPrices` і в модалці «З прайсу»).
- **Авто-субзамовлення (міграція 014).** `order_items.supplier_id` запам'ятовує постачальника позиції (з прайсу). У вкладці «Субзамовлення» кнопка **«Сформувати з товарів»** групує позиції замовлення за постачальником і створює по одному субзамовленню (`supplier_orders`, `source='auto'`) з рядками (`supplier_order_items` + `name`/`unit`), сума=Σ закупівель; позиції без постачальника → група «Без постачальника». Повторне формування заміщує лише `auto` (ручні `source='manual'` лишаються). Субзамовлення показують рядки, статус, видалення. **PDF «Замовлення постачальнику»** (`templates/supplierOrder.js`, `supplierOrderPdf()`) — кнопки 👁/⬇ на кожному субзамовленні: від компанії → постачальнику. **Без цін/сум** — лише перелік (Найменування/Од./К-сть). Блок «Для клієнта» з реквізитами (назва, ЄДРПОУ, адреса) + **тип закупівлі** (тендер/пряма, `orders.procurement_type`, міграція 018, задається в «Деталях»). Без збереження (генерація на вимогу). **Вкладка «Субзамовлення» (міграція 025):** таблиця позицій має колонку **Код** (sku) і помітку **«Замовлено»** на кожній позиції (`supplier_order_items.ordered`) + «виділити всі» (замовлені закреслені, лічильник «N з M»; позначка всіх → статус субзамовлення `ordered`). У PDF колонка «Код» уже є.
- **Замовлення: прив'язка документів** — у вкладці «Документи» картки замовлення кнопка **«Прив'язати»** (AttachDocsModal: пошук за №/файлом, за замовч. показує документи клієнта) виставляє `documents.order_id`; кнопка «Відв'язати» (order_id→null) на рядку. Документ з іншого замовлення перепризначається.
- **Склад поділено на 3 розділи за `product_type`:** **Товари** (goods — залишки, категорії, кольорова підсвітка), **Послуги** (service — БЕЗ залишків: Вихідні=out «надаємо», Вхідні=in «отримуємо», к-сть+загальна вартість з `stock_movements`), **Розхідні матеріали** (expense — придбано/списано/залишок + кнопка «Списати» = ручний out-рух `source='manual'`). Плюс таби Рухи, Збірки. **Усі три типи ведуться ОДНИМ механізмом рухів (in/out)** — `DocModal.syncDocStock` і `docgen.createStockFromDoc` більше НЕ пропускають service/expense (раніше пропускали). У розділі «Товари» показуються лише goods, тож послуги/витрати не плутають баланс. Дублі купівля/продаж під різними назвами → зливати (перенести рухи на канонічний товар + аліас + архів дубля). **Редагування/видалення:** `ProductModal` — повний редактор (назва, тип, артикул, категорія, одиниця, ціни, мін.залишок); зміна **типу** переводить позицію між розділами; «Архівувати» (`status='archived'`) і «Видалити» (лише якщо немає `stock_movements`, інакше тільки архів). Картка відкривається кліком з усіх трьох розділів.
- **Склад: збірки — перегляд/редагування/видалення.** Клік по збірці → `AssemblyDetailModal`: склад компонентів (к-сть/собівартість/сума). Редагування (назва/к-сть/дата/примітка/компоненти) = `editAssembly` (реверс рухів + перезбірка з перевіркою залишків). Видалення = `deleteAssembly` (реверс: компоненти повертаються на склад, виріб знімається — усі рухи мають `assembly_id`). Функції в `stockService`: `getAssembly/deleteAssembly/editAssembly`.
- **Склад: категорії товарів (ручні).** `products.category` (поле вже було, наповнюється вручну — Brain/прайси НЕ чіпаємо). У картці товару (`ProductModal`) — поле «Категорія» з підказками наявних (datalist) + збереження. У `StockTab` — випадайка «Категорія» (+ «Без категорії») і перемикач «Групувати» (секції за категорією з к-стю та вартістю запасу). `product_stock` view віддає `category`. Підсвітка залишків: >0 зелений, <0 червоний, 0 сірий.
- **Складські рухи для генерованих видаткових (OUT).** `saveDoc` авто-створює рухи ЛИШЕ для IN (прихідна). Для OUT (видаткова, `stockEffect='out'`) списання робить **DocGenModal**: у кроці 3 чекбокс «Списати позиції зі складу (FIFO)» (за замовч. увімкнено) → на збереженні викликає `createStockFromDoc(savedDoc.id, docType, items, …)`. (Раніше цей крок не був реалізований → видаткові НЕ списували склад; виправлено. ВН-0001 відкочено вручну скриптом — 4 позиції, cost_price=null.) `createStockFromDoc` пропускає товари з `product_type` service/expense.
- **Замовлення: генерація рахунку/видаткової** — у вкладці «Документи» кнопки **«Рахунок»/«Видаткова»** відкривають той самий `DocGenModal` (та сама логіка, що з картки контрагента/розділу Документи: збереження в `generated_docs`, складські рухи OUT через чекбокс — див. вище), але з **префілом товарів замовлення** (ціни в net + ставка ПДВ позиції) і **прив'язкою `order_id`** (`saveDoc({orderId})`, `DocGenModal` приймає `orderId`). Згенеровані показуються секцією «ЗГЕНЕРОВАНІ» у вкладці: дії на рядку — **Переглянути** (PDF у новій вкладці, `previewPdf`), **Завантажити** (`generatePdf`), **Редагувати** (DocGenModal editDoc), **Видалити**. Регенерація з даних `generated_docs`.
- **Перегляд згенерованих у розділі «Документи»/картці контрагента.** Згенеровані док-ти НЕ мають файлу у Storage (PDF рендериться з `generated_docs`), тож клік по дзеркалу відкриває **`GeneratedDocModal`** (не DocModal): зліва прев'ю PDF (регенерація в blob через `generatedDocBlob`→iframe), справа — інфо (тип/№/дата/контрагент/сума/позиції) + «Відкрити у вкладці»/«Завантажити». Якщо `getPdfBlob` не вдається — фолбек на кнопки. (Раніше клік тягнув неіснуючий файл → «Object not found».)
- **Дзеркало згенерованих у `documents` (міграція 017).** `saveDoc` тепер, окрім `generated_docs`, створює рядок у `documents` (`source='generated'`, `generated_doc_id` FK з `ON DELETE CASCADE`, PDF у Storage `generated/<id>/` best-effort) → згенеровані рахунки/накладні **зʼявляються в розділі «Документи», картці контрагента** і **впливають на борги/P&L** (видаткова=receivable; рахунок — NON_DEBT). `updateDoc` синхронізує дзеркало; видалення generated_doc каскадно прибирає його. У вкладці «Документи» замовлення дзеркало виключене з «ПРИВ'ЯЗАНІ» (показане в «ЗГЕНЕРОВАНІ»). `generated_docs.order_id` уже існує (міграція 001).
- **Замовлення: товари/позиції** (вкладка «Товари» в картці, таблиця `order_items`, міграція 009). Необов'язкові — менеджер додає, коли відомо, що входить у замовлення. Кожна позиція: `product_id` (прив'язка до довідника `products` для переюзу) + `name` (знімок назви, стійкий при видаленні товару), `qty`/`unit`/`unit_price`/`total`. Селектор `components/ui/ProductSelect.jsx` (пошук по назві/sku + «додати новий»); вільні назви при збереженні створюються/знаходяться через `resolveProduct` (aliases) → стають переюзними. **Документ КП:** додано шаблон `commercialProposal` у docgen (registry). У вкладці «КП» кнопка **«Переглянути»** на кожній версії відкриває PDF у новій вкладці (`previewPdf`→`openPdf`) — **НЕ зберігається в Документи** (за вимогою; живе лише в заявці, переглянути можна будь-коли повторним кліком). Таблиця й підсумки мають розбивку **з ПДВ / без ПДВ** (як у рахунку). **ПДВ на рівні позиції (міграції 015–016):** `order_items.vat_rate` (випадайка 0%/20%) + `price_includes_vat`. **Товари з прайсу** → `price_includes_vat=true` (ціна вже з ПДВ, без ПДВ=gross/(1+r)); **товари зі складу/довідника або вручну** → `false` (ПДВ донараховується зверху, gross=net*(1+r)). Вкладка «Товари» має колонку «ПДВ %» і **«Тип ціни» (+ПДВ зверху / ціна з ПДВ)** + підсумок «Без ПДВ · ПДВ · Всього з ПДВ». КП несе ПДВ кожної позиції (`items[].vat`), документ рахує ціну без ПДВ за ставкою позиції — тобто КП формується саме як заведені товари. **Видалення КП** (кнопка кошика на версії). КП у `NON_DEBT_TYPES`. (Є також `generateOrderDoc` — зберігає згенерований документ у `documents` з order_id — для майбутніх рахунку/накладної із замовлення.) **Маркетинговий дизайн** (на відміну від монохромних бухдокументів — це продажний документ): шапка з лого+слоганом, тонкий фірмовий лайм-акцент (`#C7F33C`, мінімально), бейдж терміну дії, блок «Чому AiM Skill» (4 переваги), заклик до дії + контакти. **Сума замовлення = сума цін продажу товарів:** при збереженні товарів `orders.total` синхронізується автоматично (Σ qty×unit_price); у вкладці «Деталі» поле «Сума» стає read-only й рахується з товарів, коли вони є (для послуг/агента без товарів — лишається ручним). **КП префіл:** «Нова версія» в КП підтягує позиції з `order_items`.
- **API для Telegram-бота (`api/bot.js`, `POST /api/bot`).** Один ендпоінт, дії через `body.action`: listOrders, getOrder, createOrder, addItems, advance, setStatus, findClient, searchPrices, overdue. Авторизація — `Authorization: Bearer <BOT_API_TOKEN>` (env Vercel `BOT_API_TOKEN`); операції через service-role (`getAdmin`). Спека для бота — `BOT_INTEGRATION.md` (модель даних, статуси, приклади). Загальний опис системи — `ОПИС_СИСТЕМИ.md`.
- **Telegram-бот (вебхук `api/telegram.js`, 24/7 у Vercel).** Майстер створення заявки (компанія→контакт→тел/email→потреба→джерело), список з пагінацією, картка з кнопкою «Відкрити в системі», пошук за номером/клієнтом, сповіщення власнику при створенні. Бот НЕ створює клієнта автоматично: шукає в базі й пропонує обрати наявного або «новий (вкажу в системі)» — заявка йде без клієнта, призначення в «Деталях» (ContractorSelect). Доступ — за `TELEGRAM_ALLOWED_IDS`; сесії в `bot_sessions`; `orders.lead_source` — джерело контакту (міграція 020). Env: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALLOWED_IDS`, `TELEGRAM_OWNER_ID`, `APP_URL`, `BOT_API_TOKEN`. Інструкція — `BOT_SETUP.md`. Завдання — `~/Downloads/ЗАВДАННЯ_БОТ_ПОТРЕБА.md`. Бот також уміє **«💵 Операція (каса/банк)»** — внесення надходження/витрати (рахунок→тип→сума→опис, validated).
- **Витяг з ЄДР (AI) у профілі контрагента (міграція 024).** У вкладці «Реквізити» кнопка **«Витяг з ЄДР (AI)»**: завантаження PDF/фото → `ai.extractCompanyExtract` (Claude) розпізнає реквізити (назва, ЄДРПОУ, ІПН, форма, адреса, директор, IBAN/банк/МФО, телефон/email, платник ПДВ) → заповнює поля форми (перевірити й «Зберегти»). Файл вантажиться у Storage `documents/edr/<contractorId>/`, шлях у `contractors.edr_extract_path`/`edr_extract_name`, перегляд через signed URL. Поряд з наявною «Заповнити за ЄДРПОУ» (vkursi/безкоштовний ЄДР API).
- **Ручне внесення операцій** у Банк/Каса → «Транзакції» → кнопка **«Додати операцію»** (`AddTxModal`: рахунок, дата, тип ±, сума, напрям, стаття, контрагент, опис; `is_validated=true`, amount зі знаком). Те саме в боті.
- **Складський рух з будь-якого документа (DocModal).** Раніше склад чіпали лише типи зі `stockEffect` (Прихідна=in, Видаткова=out) і лише при першому завантаженні. Тепер у DocModal **чекбокс «Рух на складі» + напрям (Прихід/Видача FIFO)** доступний для будь-якого документа з позиціями (напр. «Акт наданих послуг» з ліцензіями). За замовч. увімкнено для накладних, **opt-in** для актів/рахунків. Обробка **і при створенні, і при редагуванні**, ідемпотентно: `syncDocStock()` спершу видаляє старі `stock_movements` з `source='document'` для цього `document_id`, потім створює заново з `form.items`. Позиції підвантажуються з `ocr_data.items` (ContractorCard/Documents додали `ocr_data` у select), тож **старі документи виправляються простим перевідкриттям→галочка→Зберегти**. Напрям для актів дефолтиться за `doc_role`, користувач може змінити.
- **In-bot перегляд КП/документів:** скани з файлом бот надсилає; КП/згенеровані (без збереженого файлу) → кнопка «відкрити в системі». Серверний рендер pdfmake на Vercel не вийшов (бандл) — лишено на майбутнє окремим PDF-сервісом.
- **Прайс постачальника по API — Brain (`api/brain-sync.js`, міграції 022–023).** Окрім Excel-імпорту, прайс тягнеться з API. Контрагент Brain = ТОВ «КОМЕЛ» (ЄДРПОУ 24083083, id `c97b8dea-1e03-4743-9cbf-1e403a59d13b`). **Метод: `/products` по обраних категоріях** (повний прайс-файл `/pricelists` повертав 0/невідома структура — відмовились). Дії ендпоінта (`body.action`): `categories` (дерево категорій з `/categories` + поточний вибір), `save_categories` (зберегти у `supplier_price_lists.categories` jsonb), `sync` (default). Синк: `POST /auth` (login+md5) → SID; `/categories` + `/vendors` (vendorID→бренд); для кожної обраної категорії (з розгортанням на всіх нащадків, `expandCategories`) — пагінація `/products/{categoryID}/{SID}?limit=100&offset=N`; мапінг (articul/product_code→sku, price_uah→price, retail_price_uah→retail_price, vendorID→brand, stocks/available→in_stock) → **перезапис** `supplier_prices` для price_list з `source='brain_api'`. UI: кнопка **«Категорії»** (модалка `BrainCategoriesModal` — дерево чекбоксів+пошук, вибір батька включає підкатегорії) і **«Оновити з API»** (активна лише коли є обрані категорії). Запит з серверлес (CORS+http). Тригери: кнопка (Bearer supabase-JWT) + **щоденний cron** `0 5 * * *` (Bearer `CRON_SECRET`). Env: `BRAIN_LOGIN`, `BRAIN_PASSWORD`, `BRAIN_SUPPLIER_ID` (опц., інакше пошук/створення за `BRAIN_SUPPLIER_NAME`='Brain'). `BRAIN_TARGET_ID` більше не потрібен. Пошук/«З прайсу» в замовленні — без змін (та сама таблиця).

**Липень 2026 (зроблено):**
- **Звіт «Рентабельність» (Аналітика).** По видаткових накладних (як ООН-таблиця): постачальник + джерело закупівлі, згортні накладні, компактний вигляд. Клік по видатковій (заголовок) / прихідній (джерело) відкриває документ. Собівартість = **за останньою закупівлею** (див. §6.16). **Послуги без собівартості → COGS=0** (агентські/консультаційні — Гігаклауд, ГАЛИЧФАРМ мали хибну маржу). **Закупівлі без ПДВ (неплатники):** звіт визначає закупівлю без ПДВ за `vat_amount`+прапорцем `is_vat_payer` (виправлено 20 хибних прапорців). Знак мінус для збиткової маржі/чистого показується коректно (раніше `fmtInt` відкидав знак), кольори доходи-зелені/витрати-червоні.
- **«Розрахунок для інвестора» (PDF, із замовлення).** Рентабельність, капітал, чистий прибуток, ROI — для рішення про участь у закупівлі. Монохромний фірмовий стиль (як видаткова), компактно на 1 сторінку.
- **Опційна печатка** — галочка «З печаткою» у генерації документів і в КП (печатка зсунута вправо/вгору, ~42мм, щоб не перекривала підпис).
- **Повні реквізити контрагента у ВСІХ шаблонах docgen** — адреса/IBAN/банк/директор підтягуються у кожен шаблон (раніше лише в частину). У тексті договору директор контрагента з fallback `contact_person→director`. Фікс банку й директора в договорі фіндопомоги.
- **Видалення згенерованих документів** — кнопка у розділі «Документи».
- **Витяг з ЄДР — «Розпізнати ще раз»:** перерозпізнати вже збережений файл без повторного завантаження; реквізити зберігаються навіть без колонок `edr_extract`; kved→primary_kved; директор ≠ засновники (уточнено промпт).
- **Склад:** об'єднання товарів (поєднати закупівлю і реалізацію під різними назвами), ручні рухи, видалення окремого руху з картки товару (прибрати задвоєний прихід), перемикач «Показати архівні» в усіх 3 розділах (+бейдж «архів»). Fix: `resolveProduct` більше не зліплює різні моделі нечітким матчем (Ajax-датчики). Перерозпізнавання документа замінює ВСІ його рухи (крім збірок) — без задвоєння.
- **Замовлення: «Копіювати»** — дублює замовлення з усіма товарами.

**Закриття періоду — фази 1–3 (розділ «Закриття періоду», `/period-close`, деталі §6.18):**
- **Фаза 1 + жорстке блокування:** чек-лист якості → знімок → upsert; тригери БД `PERIOD_CLOSED` (діють навіть на service-role). Переоткриття (admin) знімає блок.
- **Фаза 2 — безперервність залишків:** на початок + рух = на кінець (склад і гроші).
- **Фаза 3 — діф знімків** при повторному закритті (що змінилось).
- **Drill-down «Документи й операції»:** продажі/закупівлі з позиціями + транзакції; розбивка транзакцій за статтями; клік по статті розкриває транзакції (дата·контрагент·сума). **Інлайн-класифікація** некласифікованих транзакцій прямо в чек-листі. У складський залишок/оцінку/чек-лист — **лише товари (goods)**, не послуги/розхідники. Коректний напис для документів без позицій (послуга/рахунок ≠ дірка; ⚠ лише для товарних накладних).

- **Fix деплою:** авто-перезавантаження при застарілому lazy-чанку після деплою (`vite:preloadError` + ErrorBoundary reload).

- **Видалення документа тепер відкочує його складські рухи (2026-07-14).** FK `stock_movements.document_id` = **ON DELETE SET NULL** (перевірено живим тестом) — тож видалення документа рухи НЕ прибирало, вони осиротіли (document_id→null), а склад не відкочувався (фантомний прихід/видаток). `DocModal.del()` і `OrderCard.delGen` тепер явно видаляють пов'язані рухи (крім збірок `source='assembly'`) перед/під час видалення — як уже робив `GeneratedDocModal`. Порядок безпечний: спершу видаляємо документ (перевірка прав/періоду), лише тоді рухи. **Розбір 14 осиротілих рухів (2026-07-14, зроблено):** 13 з них — легаси-рухи збірок (Tableau Kit, Canon-комплект, відеомонтажний ПК) зі старим `source='document'` та `assembly_id=null` — перейменовано `source→'assembly'` (кількості не чіпані); 1 — справжній сирота від видаленої видаткової (Монітор 24E1N1100A) — видалено фантомне списання, баланс −1→0. Осиротілих `source='document'` з `document_id=null` більше немає (0). **Помічено побіжно:** до монітора 24E1N1100A прилип продаж «Монітор 24 Philips» (окреме злипання, як SafePal/Godox — у бек-лозі аудиту аліасів).

**Задачі + Telegram-нагадування (2026-07-20).** Розділ **«Задачі»** (`/tasks`, `pages/Tasks.jsx`, **міграція 030** — таблиця `tasks`: title, description, due_date, status open|done, priority, source app|bot, reminded_on). Список/фільтр (активні/виконані/усі), створення/редагування/виконання/видалення, підсвітка прострочених і на сьогодні. **Бот** (`api/telegram.js`): кнопки «✅ Задачі» (список, клік→картка з «✅ Виконано»/«🗑») і «➕ Задача» (майстер: текст → термін; дати «сьогодні/завтра/+Nд/YYYY-MM-DD/-»). **Нагадування:** `api/task-reminders.js` + cron `0 6 * * *` — щодня шле власнику (`TELEGRAM_OWNER_ID`) прострочені + на сьогодні, мітить `reminded_on` (без дублів). Авторизація cron — `CRON_SECRET` (Vercel додає Bearer).

**Банк: авто-створення контрагентів при імпорті виписки (2026-07-19).** Раніше транзакція з незаведеною компанією імпортувалась із назвою, але `contractor_id=null` (контрагент не створювався) → не впливала на борги/aging/tx↔doc. Тепер `ImportTab.doImport` авто-створює контрагента за **ЄДРПОУ** для транзакцій без збігу. **Захист від дублів:** ключ = ЄДРПОУ (унікальний, не назва); (1) свіжа перевірка існуючих у БД за ЄДРПОУ → переюз; (2) один запис на ЄДРПОУ в межах пачки; (3) наша компанія 45505924 пропускається. Новий контрагент: назва з виписки + ЄДРПОУ + is_supplier/is_client за напрямом (Витрати/Доходи). Чекбокс «Автоматично створювати контрагентів за ЄДРПОУ» (за замовч. увімкнено) + лічильник. Транзакції без ЄДРПОУ лишаються без контрагента (за назвою надійно не дедуплікувати). `txEdrpou` експортовано з contractorMatch.

**AI-імпорт специфікації товарів у замовлення (2026-07-17).** Вкладка «Товари» замовлення → кнопка **«Специфікація (AI)»**: завантажуєш договір/специфікацію (**.docx** / PDF / фото) → AI витягує товарні позиції (назва, к-сть, од., ціна, ПДВ) → додаються рядками (без ручного вводу). `extractOrderItems` (lib/ai.js): .docx → текст через **fflate** (`docxToText`: unzip `word/document.xml` → strip тегів) → Claude структурує; PDF/фото → Claude напряму. `priceIncludesVat` визначається з колонки таблиці. Собівартість = 0 (заповнити «З прайсу»/вручну). Нова залежність: `fflate`.

**Генеровані документи: прикріплення реального скану (2026-07-17).** У розділі «Документи» генеровані відкривались у `GeneratedDocModal` (рендер PDF з даних; прев'ю часто «недоступне» через pdfmake-blob). Додано кнопку **«Завантажити скан»** — прикріплює підписаний PDF/фото до генерованого документа (оновлює `documents.storage_path/file_name/file_type`). Після цього документ відкривається як **звичайний** (`DocModal`: прев'ю + розпізнавання + перевірка). `Documents.openRow`: генерований з реальним сканом (storage_path не з `generated/`) → DocModal; без скану → GeneratedDocModal.

**Авто-нормалізація назв файлів (2026-07-16).** Після OCR нового скану назва файлу будується автоматично: «Тип №Номер Контрагент Дата.ext» (замість сирих IMG_/GUID). `buildDocFileName` у DocModal (очищає лапки/пунктуацію, зберігає розширення). Кнопка «оновити назву» біля поля перебудовує з поточних полів; existingDoc не перейменовується при повторному розпізнаванні. Стосується головного шляху (скан документа); інші аплоади (пошта/витяг з ЄДР/вкладення в DocGenModal) — за потреби.

**Fix: OCR HTTP 413 — стиснення зображень (2026-07-16).** Проксі `/api/ai` має ліміт ~4.5МБ на тіло; фото з телефону + base64 його перевищували → HTTP 413. `normalizeImage` (lib/ai.js) тепер зменшує великі зображення до 2200px (довший бік) + JPEG 0.82 перед base64 — застосовується в обох OCR-шляхах (`extractDocumentMulti`, `extractCompanyExtract`); маленькі JPEG не чіпаються. `callClaude` дає зрозуміле повідомлення при 413. (PDF client-side не стискаються — для завеликих PDF повідомлення радить розділити на сторінки.)

**🔴 СИСТЕМНИЙ FIX: збереження документа стирало складські рухи (2026-07-15).** Корінь усіх «зниклих рухів» (MSI №1403, BLUETTI, ПРООН №3, ГАЛИЧФАРМ тощо): `DocModal.syncDocStock` при КОЖНОМУ збереженні спершу **видаляв усі рухи** документа, а створював заново лише якщо увімкнено «Рух на складі» (`stockOn`). Документ з порожнім OCR → `stockOn=false` (бо `items.length===0`) → рухи видалялись і не відновлювались. Тобто відкрити такий документ і натиснути «Зберегти»/«Розпізнати» = втратити прихід/видаток. **Fix:** ранній `if (!stockOn) return` ПЕРЕД `delete` — коли рух вимкнено, наявні рухи не чіпаються. MSI B760 відновлено (IN 1 @6082.5, баланс −1→0). Це пояснює, чому доводилось відновлювати рухи вручну.

**Аудит: видаткові з сумою без складських рухів (2026-07-15, `migrations/audit_sales_no_stock.mjs`).** Крім BLUETTI — 2 видаткові без рухів: (1) **№6 ГАЛИЧФАРМ** — ПЗ Data Sec ×290 (481к): реальний втрачений продаж, залишок був +290 → відновлено OUT 290 @1380.58 (cost 1170.04) → баланс 0; (2) **№1 ПРООН (16.02, 90.5к)** — ті самі 4 фото-товари, що видаткова №2 ПРООН (19.02, 86к), але в наявності по 1 екз кожного → №1 і №2 = той самий продаж двома документами. №2 реальний (має рухи), №1 зайвий (рухів нема, склад не задвоєний, але сума 90.5к задвоювала дохід/дебіторку) — **№1 видалено** (без рухів/оплат/прив'язок, чисто).

**Дані: відновлено втрачений рух продажу BLUETTI Apex 300 (2026-07-15).** Баланс +4 (куплено 4, продаж «зник»). Видаткова №3 ПРООН від 2026-04-07 (416000 = 4×BLUETTI) існує, але її рух списання загубився (ocr_data.items очистились → рух зник). Відновлено OUT 4 @86666.67 (cost 75219) на цю видаткову → баланс 0. (Клас проблем «видаткова є, а рух не створився/загубився» — як Faraday Mojave; трапляється коли позиції документа очищаються.)

**Fix: собівартість у «Рентабельності» — фолбек на останню закупівлю (2026-07-15).** Багато OUT-рухів мали `cost_price=null` (не записалась при продажу) → звіт показував собівартість 0, маржа = весь продаж (монітор, панель, Samsung тощо, хоча прихід із ціною є). `salesProfitReport`: коли `cost_price` руху порожній — підставляє `lastInNet` (ціна останньої закупівлі, net; рішення #16). Послуги/агентські COGS=0 (коректно), товари без приходу — 0 (законно). Фолбек самовідновний. Опційно можна ще й персистити cost_price на рухах скриптом (не зроблено — фолбек достатній для звіту).

**Дані: розщеплення ноутбука ASUS Vivobook (2026-07-15).** «Прихід не створився» для Чорного E510KABQ1195 — насправді створився, але на товар-двійник «E510KA**-**BQ1195» (покупка з дефісом у назві, продаж без) → канон −8, двійник +8. Об'єднано: рух перенесено на канон, двійник в архів, додано аліаси (з дефісом і без), прибрано сміттєвий аліас «Блакитний». Блакитний (1236) був у порядку (його аліаси вже містили дефісну назву). Ще один приклад злипань/розщеплень з аудиту аліасів (корінь — старий нечіткий resolveProduct).

**Fix: авто-нумерація генерованих документів (2026-07-15).** `getNextDocNumber` брало номер **останнього СТВОРЕНОГО** документа +1 → при беквдейті/ручних правках/видаленнях останній міг мати менший номер → колізія (два ВН-0003). Виправлено на **max(PREFIX-NNNN)+1** серед документів цього типу. Існуючий дубль виправлено: новіший ВН-0003 (СУМИСПЕЦ, 15.07) перейменовано на ВН-0004 (з дзеркалом у documents і назвою файлу).

**Fix: маржа товарів замовлення — net-to-net (2026-07-15).** Маржа рахувалась як `(unit_price − cost_price)` по валових цінах → завищена сума. Тумблер «Тип ціни = ціна з ПДВ» (`price_includes_vat`) стосується **і продажу, і закупівлі** — тоді ПДВ віднімається з обох (`netUnit`/`netCost`). Приклад: продаж 19999, закупівля 14805 (обидва з ПДВ) → маржа `(16665.83−12337.50)×12 ≈ 51940` (26%), а не gross 62328. (Відсоток при однаковій ставці ПДВ не міняється — міняється лише сума з gross на net.) Звіт «Рентабельність» (`salesProfitReport`) рахує коректно (net−net) — не чіпано.

**ПДВ + перевірка документів + ворота закриття періоду (2026-07-14).** Аудит ПДВ (`migrations/audit_vat.mjs`): математика ПДВ **коректна** (0 аномалій, скрізь vat=сума/6). Компанія була помилково `is_vat_payer=false` — **виправлено на true** (`tax_system='general'`). Додано: **звіт ПДВ** у «Закритті періоду» (податкове зобов'язання з продажів − податковий кредит із закупівель = до сплати, з розшифровкою по документах; управлінський рівень, дані з `documents.vat_amount`). **Перевірка документів** — `documents.is_verified` (**міграція 029**): тумблер «Перевірено» в `DocModal` (звірка скан↔розпізнані поля/ПДВ/рухи), бейдж+фільтр «Перевірка» у реєстрі Документів. **Ворота закриття** (`runChecklist`): блокери тепер = мінусові залишки + док без сум + **неперевірені документи** + **невалідовані транзакції** (раніше tx були лише попередженням). Неперевірені документи в чек-листі клікаються → відкривають DocModal для звірки. Код ґрейсфул до застосування 029 (реєстр Документів і чек-лист не ламаються). **Звіт ПДВ в Аналітиці (зроблено):** вкладка «ПДВ» (`vatReport(year)` у periodClose.js) — KPI за рік + таблиця по місяцях із розкриттям документів по кліку (клік по документу → DocModal). **Лише реалізовані документи** (накладні/акти, `countsAsDebt`) — рахунки/замовлення не задвоюють ПДВ (те саме у блоці ПДВ закриття періоду). **Перенос від'ємного значення:** надлишок кредиту (кредит>зобов'язання) накопичується й зменшує «До сплати» наступних місяців — рахується по всій історії (січень може мати перенос з попереднього року). Колонки: Зобов'язання/Кредит/Перенос з мин./До сплати/Залишок кредиту→. **Фікси:** KPI показував NaN (передавали готовий рядок у Kpi, що робить fmtInt повторно); `fmt` віддає abs — мінус додається вручну (`si`). **Цифровий аналог документа в DocModal:** справа від скану — таблиці «Розпізнані позиції» (з `ocr_data.items`) і «Складські рухи» (фактичні рухи документа, з підсвіткою неприв'язаних товарів і попередженням, якщо позиції є, а рухів нема). **Наступні кроки (не зроблено):** можливо, розрізнення податкових накладних; експорт звіту ПДВ.

**Дані: розбіжність doc_role↔direction у 2 документах (2026-07-14).** Закриття періоду показувало Акт наданих послуг №61714 (Вчасно Сервіс) як продаж, а не закупівлю. Причина: `computePeriodDetail` класифікував лише за `direction`, а №61714 мав `doc_role=incoming` (правильно) але `direction=receivable` (помилка). **Код-фікс:** класифікація за `doc_role` (як у `vatReport`), select тягне `doc_role`. **Аудит:** лише 2 документи з розбіжністю role↔direction — виправлено дані: №61714 (Вчасно Сервіс, постачальник, послуга нам) → `direction=payable` (закупівля); №3 waybill (ТОП ДЕФЕНС-ПРО, клієнт, 24 видаткові рухи, продаж 2.15М, ПДВ 359к) → `doc_role=outgoing` (продаж, а не кредит). Тепер обидва екрани (ПДВ в Аналітиці й закриття) узгоджені. (Фікс пройшов бо періоди 2025-02…10 були перевідкриті користувачем — замок працює.)

**Дані: аудит дублів документів → подвоєння складу (2026-07-14, `migrations/audit_dup_docs.mjs`).** Групування за контрагент+номер. Крім MSI знайдено ще **2 справжні дублі з подвоєнням складу** — обидва вхідні, з копією, повторно завантаженою сьогодні як .PDF (оригінали .jpg з оплатою): **НЕБИЛОВИЧ №1043** (Провід +2, Термостат +1) і **ЛАН СТОР №929** (Keystone +200). Дублі видалено (`migrations/fix_dup_docs_0714.mjs`) → баланси 0. Збіги номерів у ПРООН (№1–4) — НЕ дублі (різні продажі/дати/суми). **Окремо (не чіпано, не подвоюють склад, але подвоюють дохід/дебіторку):** вихідні дублі **ТОП ДЕФЕНС-ПРО №4/1** і **№6** (2025-06, той самий день+сума, рухи лише в одному) — на ручний огляд (червень 2025 «закритий»).

**Дані: дубль закупівлі MSI B760 (2026-07-14).** Материнка «MSI B760 GAMING PLUS WIFI …4SATA/2M.2» висіла в залишку 1, хоча все продано. Причина: рахунок №1403 ТОВ «ТМР» (1 шт, 7299) заведено **двічі** (оригінал 330140d2 від 14.06 з оплатою; дубль 7afb836a створений 14.07, gross-ціна, без оплати). Видалено дубль-документ + його рух → баланс 0 (куплено 9 = продано 9). (Операції пройшли бо періоди 2025-02…10 на той момент були перевідкриті — замок закритого періоду справний, підтверджено користувачем 2026-07-14.)

**Дані: рахунок №1 ПРООН — флешки (2026-07-14).** Рахунок-фактура №1 (bc798d07, 25.02.2026) мав 11 позицій, а склад списав лише 10. Причини (звірено з PDF рахунку): (1) **datAshur 16GB** (рядок 6, @6550) не списався — обидва рядки datAshur (16GB+128GB) розпізналися в один товар «datAshur 128GB» через аліас «…16 GB» на ньому, рух 16GB загубився → 16GB висів 1; (2) **IronKey D300S 128GB** списався на фантом «IronKey D500S 128GB» (друкарська помилка в рахунку; купували D300S 128GB @10415) → D500S = −1, D300S 128GB висів 1. Фікс (`migrations/fix_proon_flash.mjs`): створено пропущений рух datAshur 16GB out @6550, перенесено рух @33000 з D500S→D300S 128GB, D500S заархівовано. Усі 4 флешки тепер 0. **Корінь усунено (2026-07-14, `migrations/fix_datashur_faraday.mjs`):** з товару «datAshur 128GB» прибрано аліаси «16 GB» + дублі (лишилось 2×128GB), товару «datAshur 16GB» додано власні 16GB-аліаси — тепер ємності не зліплюються. **Faraday Mojave bag:** продаж ніде не створив руху (Faraday-OUT'и загалом навіть криво прив'язані до вхідної накладної №16 ДАТАВЕЙ; точну видаткову/ціну з даних не відновити) — створено коригуючий OUT `source='manual'` ×1 @2915 (ціна=собівартість, маржа 0, **ціну продажу уточнити вручну**) → баланс 0.

**Дані: розлиплення SafePal / CoolWallet (2026-07-14).** При закритті листопада 2025 виявлено нестиковку складу: товар «SafePal S1 Pro» через сміттєві аліаси (містили назви CoolWallet) «всмоктав» прихід CoolWallet Pro з накладної №251 → SafePal показував +2 (мав +1), CoolWallet мав залишок **−1** (продали, а прихід пішов на SafePal), оцінка складу завищена на 666,67, собівартість SafePal = 2575 замість 1908,33. Фікс (`migrations/fix_safepal_coolwallet.mjs`): перепризначено прихідний рух на CoolWallet, почищено/дедупнуто аліаси SafePal, додано коректні аліаси CoolWallet, собівартість за останньою закупівлею (SafePal 1908,33 / CoolWallet 2575), видалено 2 архівні дублі-порожняки. Обидва товари тепер сходяться в 0. **Ризик:** аналогічне «злипання» могло статися й з іншими товарами (наслідок старого нечіткого матчу в `resolveProduct`, вимкненого коммітом f8cecfa) — за потреби провести аудит аліасів по всіх товарах.

**Генерація документів — фікси (2026-07-14):**
- **Поля видаткової/акту більше не «злітають».** Причина: `generated_docs` не мала колонок `invoice_ref/invoice_ref_date/delivery_basis/delivery_address` — `saveDoc`/`updateDoc` їх не зберігали, а `GeneratedDocModal`/`OrderCard.genOptions` читали `d.invoice_ref` → завжди `undefined`, тож при редагуванні/регенерації Рахунок №, дата рахунку, базис і адреса поставки губилися. **Міграція 027** + `saveDoc`/`updateDoc` тепер зберігають ці поля (з ґрейсфул-фолбеком, якщо міграцію ще не запущено).
- **Вибір рахунку у видатковій/акті.** У DocGenModal над «Рахунок №» зʼявилась випадайка «Обрати рахунок» зі списку наявних згенерованих рахунків контрагента (`doc_type='invoice'`) — вибір заповнює номер+дату; ручне введення лишається.
- **Договори видно в картці контрагента.** Раніше `contractor_contracts` використовувалась лише в DocGenModal (завантаження списку + авто-створення), але ніде не показувалась. Додано блок **«Договори»** у вкладку «Реквізити» (`Contracts` у ContractorCard) — перегляд/додавання/видалення; авто-створені при генерації документа договори тепер видимі.
- **Результат замовлення при архівуванні (виграно/програно).** Для тендерних/конкурсних: `orders.outcome` ('won'|'lost'|null, **міграція 028**). У картці замовлення кнопка «Архівувати» → вибір **Виграно / Програно / Без результату** (`archive(outcome)`; `unarchive` — окремо; ґрейсфул-фолбек, якщо колонки ще нема). Бейдж результату в шапці картки і в реєстрі (стовпець «Статус»); у банері архіву — перемикач результату (`setOutcome`). У фільтрі «Архів» — підсумок **Виграно/Програно + суми + win-rate**. `OUTCOME` винесено в `lib/orders.js`.
- **Реквізити контрагента: перегляд за замовчуванням, редагування — за кнопкою.** Раніше вкладка «Реквізити» відкривалась одразу в режимі редагування (усі поля — інпути) → можна випадково змінити дані, і «стіна» порожніх полів. Тепер за замовчуванням **компактний перегляд**: заголовок (назва/ЄДРПОУ/ІПН/форма + бейджі клієнт/постачальник/ПДВ) і щільна сітка **лише заповнених** полів; контакти/договори — read-only. Кнопка **«Редагувати»** відкриває форму (як раніше) з **«Зберегти»/«Скасувати»** (Скасувати повертає початкові значення). `editing`-стан у `DetailsTab`; `Contacts`/`Contracts` приймають `readOnly`.

**Fix: у видатковій/акті випадайка «Договір» показувала лише один договір (2026-07-29).** Договори, заведені через **Документи → розпізнавання**, лежать у таблиці `documents` з `type` = договірний (`supplyAgreement`/`loanAgreement`), а `DocGenModal` тягнув список **лише** з реєстру `contractor_contracts` (куди потрапляли тільки авто-створені за номером). Тому для контрагента з кількома договорами-документами (напр. ГУНП: №1411/1413/1414/1415) у дропдауні був лише №1411. **Fix (`DocGenModal.jsx`):** список «Договір» тепер об'єднує **обидва джерела** — `contractor_contracts` + договори-документи з `documents` (типи з прапорцем `isContract`), дедуп за номером. Додано опцію **«+ Інший договір…»** для ручного вводу нового номера/дати (раніше при наявності хоча б одного договору поля вводу зникали). При збереженні документа обраний договір-документ або новий вручну **авто-реєструється** в `contractor_contracts` за номером (fake-id `doc:*` не пишеться в `contract_id`). Зміни лише фронтенд, без міграцій.

**КП: переверстано шаблон під офіційний діловий лист (2026-08-27).** `commercialProposal.js` — новий макет за прикладом користувача: шапка (лого зліва · реквізити компанії справа з лінією під назвою) → повна лінія → «№ … від … / Пропозиція дійсна до …» → блок «КОМУ» (з відступом праворуч) → центрований заголовок «КОМЕРЦІЙНА ПРОПОЗИЦІЯ» з лаймовим акцентом → вступ (justified, leadingIndent) → таблиця товарів → підсумки (Всього з ПДВ на світло-зеленому) → підпис «З повагою / Директор» + М.П./печатка → заклик-контакти (лаймовий лівий акцент). Прибрано старий маркетинговий блок «Чому AiM Skill» і бейдж-термін. Лише PDF; xlsx без змін.

**Документи-чернетки в замовленні: «провести по системі» (2026-08-27, міграція 038 — запустити вручну).** `documents.posted` (bool, default true). Файл, завантажений у замовлення («Завантажити»), тепер стартує **чернеткою** (`posted=false`, `!orderId` у `DocModal`) — видно **лише в замовленні**, НЕ в розділі «Документи» й картці контрагента. Чекбокс **«Провести по системі»** у `DocModal` (+ per-row кнопка «Чернетка/Проведено» у вкладці «Документи» замовлення, `setPosted`) додає документ у загальний реєстр. Фільтр `posted IS NOT false` (ґрейсфул до 038) у: реєстрі Документів (`Documents.jsx`), картці контрагента (`ContractorCard.jsx`). `posted` ґрейсфул у insert/update DocModal. **Облік (баланси через в'юху `contractor_balances`, P&L, aging) поки НЕ фільтрує posted** — облік чернеток окремою фазою (потребує оновлення в'юхи, визначення поза міграціями). Наразі чернетки — це переважно договори/ТТН (NON_DEBT), тож впливу мало. Міграція 038: колонка + індекс (DEFAULT true → наявні документи лишаються проведеними).

**Замовлення → Документи: завантаження власних файлів (2026-08-27).** У вкладці «Документи» кнопка **«Завантажити»** — прикріпити довільний файл (договір, ТТН, акт тощо) у замовлення. `DocModal` отримав проп `orderId` (пишеться в `documents.order_id`) і **режим без OCR** (`autoOcr={false}`): файл прикріплюється, тип/номер/дата задаються вручну (не ганяємо Claude на договорах, які він однаково класифікує невірно). Дропзона приймає PDF/фото/DOCX/XLSX. Хелпер `defaultForm` + фолбек: якщо OCR падає — форма все одно дає заповнити вручну (не блокує збереження). Документ з'являється у «ПРИВ'ЯЗАНІ» й у розділі «Документи». Доповнює наявні «Рахунок»/«Видаткова» (генерація) і «Прив'язати» (наявний документ).

**Товари замовлення: колонки за вимогою + постачальник у рядку (2026-08-26).** Основні колонки таблиці тепер: Назва/код · К-сть · Закупка · Ціна · Націнка% · **Тип ціни** (з ПДВ/+ПДВ) · Сума · Маржа(грн+%) · **Постачальник**. «Тип ціни» винесено з розкривного блоку в основний рядок. Додано колонку **Постачальник** — select наявних (`contractors.is_supplier`) + опція «＋ новий…» (`chooseSupplier` → window.prompt → створює контрагента is_supplier і ставить у рядок). Розкривний блок (`⋯`) лишає Од., ПДВ %, Без ПДВ, прибрати. `supplier_id` зберігається (для авто-субзамовлень). minWidth таблиці 1040 (горизонт. скрол на вузьких).

**Замовлення: відповідальний менеджер (2026-08-25, міграція 037 — запустити вручну).** `orders.manager_id` (FK → profiles). Вибір «Відповідальний менеджер» у вкладці «Деталі» (`DetailsTab`) і при створенні (`NewOrderModal`, за замовч. — поточний користувач). У реєстрі (`Orders.jsx`) — колонка «Менеджер» (сортована) + фільтр **«Мої»** (замовлення на поточного користувача). Імена підтягуються окремим запитом `profiles` (не embed → не ламається до міграції). Save/insert ґрейсфул, якщо колонки ще нема. **Міграція 037** додає колонку + політику `profiles_select_all` (усі авторизовані бачать список користувачів — для дропдаунів).

**Товари замовлення: імпорт з таблиці Excel/CSV (2026-08-25).** У «Дії» пункт **«З таблиці (Excel/CSV)»** — завантажуєш файл із колонками Назва + К-сть (опц. Код, Ціна, Од.) → `importTable` (динамічний `import('xlsx')`) розпізнає заголовки (укр/англ синоніми, фолбек: назва=перша колонка), додає рядки позицій. Ціни/собівартість заповнюються далі («З прайсу»/«Собівартість (AI)»/вручну), на збереженні `resolveProduct` лінкує до довідника. Швидка альтернатива ручному вводу й AI-специфікації.

**Товари замовлення: щільна таблиця замість карток (2026-08-25).** `ItemsTab` (`OrderCard.jsx`) переписано на компактну grid-таблицю за макетом-прикладом: один рядок на позицію (назва + код-моно у 1-й колонці), інлайн-редаговані комірки (класи `.cell-input`/`.cell-name` у styles.js — виглядають як текст, рамка на hover/focus), колонки Назва/К-сть/Закупівля/Націнка/Ціна/Сума/Маржа. Рідкісні поля (Од./ПДВ/Тип ціни/Без ПДВ + «прибрати») згорнуті — розкриваються кнопкою на рядку (`expanded` стейт). Шапка «Позиції · N шт» + масова націнка + «Дії»/«позиція». Липкий підсумок унизу (Собівартість · Виручка без ПДВ · ПДВ · Маржа · Всього з ПДВ ₴). `AutoTextarea`/`ProductSelect` отримали проп `className`. Горизонтальний скрол на вузьких екранах (minWidth 800).

**Користувачів додає лише адмін, самореєстрацію вимкнено (2026-08-25).** `components/Auth.jsx` — прибрано режим реєстрації (лише вхід; підказка «зверніться до адміністратора»). Новий ендпоінт **`api/create-user.js`** (service-role): перевіряє, що викликач — admin, тоді `auth.admin.createUser({email_confirm:true})` + upsert `profiles` з роллю. У **Налаштування → Користувачі** кнопка **«Додати користувача»** (email/пароль/ім'я/роль) — акаунт готовий одразу без листа-підтвердження. **Рекомендація:** у Supabase → Authentication вимкнути «Allow new users to sign up» (admin API це не зачіпає), щоб заблокувати самореєстрацію і на рівні API.

**Рольова модель доступу — UI-рівень (2026-08-25, міграція 035 — запустити вручну).** Замість «усі ролі = повний доступ» тепер розділи ховаються за роллю. Єдине джерело правди — `src/lib/permissions.js` (`ROLE_SECTIONS`, `canAccess`, `firstSection`, `ROLE_LABELS/ROLE_HINTS`). Матриця: **admin** — усе; **Бухгалтер** (accountant) — усе крім Налаштувань; **Менеджер** (manager) — Замовлення/Задачі/Контрагенти/Склад/Прайси/Документи/Пошта; **Перегляд** (viewer) — Замовлення/Контрагенти/Аналітика. `Layout.jsx` фільтрує сайдбар і мобільну навігацію; `App.jsx` — `Gate` на кожному роуті (пряме введення URL → редірект на перший доступний розділ) + `Home` (старт за роллю). Екран **Налаштування → Користувачі**: українські назви ролей + опис доступу; `setRole` тепер показує помилку, якщо оновлення зачепило 0 рядків (RLS). **Міграція 035** дає адміну право бачити всіх і міняти ролі (`is_admin()` SECURITY DEFINER + політики select/update на `profiles`) — стара схема мала лише «update own». **Захист лише в UI** (за вибором користувача): дані в БД відкриті авторизованим, RLS-замок — окрема майбутня фаза. Нові користувачі самі реєструються → «Перегляд», адмін призначає роль.

**Реєстр замовлень: сума з ПДВ (2026-08-19, міграція 034 — запустити вручну).** Колонку «Сума» перейменовано на **«Сума з ПДВ»** (`Orders.jsx`). Причина розбіжності: нова вкладка «Товари» пише `orders.total` = сума з ПДВ (gross), а стара «Деталі» раніше писала без ПДВ для позицій `price_includes_vat=false` → частина замовлень мала net. **Міграція 034** перераховує `orders.total` = сума з ПДВ з `order_items` (та сама формула, що у «Товарах»); замовлення без позицій (ручна сума) не чіпаються. Після цього реєстр/шапка/KPI показують gross усюди. UI-макет картки товарів також перероблено на картку (назва повним рядком + поля з підписами під нею; сума з ПДВ у рядку назви).

**Замовлення: степер статусів у шапці + перероблена «Деталі» (2026-08-18, міграція 033 — запустити вручну).** Статус тепер керується **степером у шапці картки** (`StatusStepper` у `OrderCard.jsx`) — 6 клікабельних етапів, пройдені зелені з галочкою, поточний синій; клік = миттєвий перехід (`setStatus`), «Закрито» ставить `closed_at`, вихід із «Закрито» скидає. Бейдж статусу з шапки прибрано. **Вкладка «Деталі»** перероблена: прибрано «Суму» (є в шапці й «Товарах») і «Статус» (тепер у степері); лишились Клієнт, Тип закупівлі, Опис; додано **Ідентифікатор закупівлі** (`orders.procurement_id`, поле з'являється лише коли Тип закупівлі = «Тендер»), **Дата створення** (read-only, `created_at`) і **Дата закриття** (редагована, `closed_at`). `DetailsTab` пересинхронізується при зміні статусу зі степера (`key`). **Міграція 033** додає `orders.procurement_id text` (save ґрейсфул, якщо колонки ще нема). Уточнено: у картці **7 вкладок** (не 5 — рання нотатка про «приховані» Транзакції/Склад була хибною).

**Замовлення: спрощено статуси до 6 (2026-08-18, міграція 032 — запустити вручну).** Замість трьох окремих статус-машин (12/4/5 кроків) — **єдиний набір для всіх типів**: `new` Новий · `processing` В опрацюванні · `ordering_supplier` Замовлення у дистрибютора · `shipped` Відвантажено · `paid` Оплачено · `closed` Закрито. `lib/orders.js`: `FLOW` = один `STAGES` для trade/service/agent; додано `LEGACY`-мапу (старі ключі → підпис нового набору), `statusLabel`/`labelForStatus` через неї, тож наявні замовлення читабельні ще до міграції. `proposalOverdue` тепер на статусі `processing`. Узгоджено бота: `api/bot.js` (FLOW+STATUS_LABEL) і `api/telegram.js` (STATUS_LABEL). КП-надсилання рухає `new→processing` (раніше `new→proposal_sent`, лише trade — тепер усі типи). **Міграція 032** переназначає наявні замовлення (proposal_sent/confirmed/contract_signed/invoiced/paid_partial/client_transferred/deal_done→processing; in_transit/ready_to_ship→ordering_supplier; docs_received→shipped; решта без змін). Таблиця orders не під тригером періоду, UPDATE проходить. Доповнює запис нижче про прибрану «Наступну дію».

**Замовлення: прибрано «Наступну дію» (2026-08-18).** Концепцію визнано надлишковою — статус тепер єдиний контрол стану заявки. Прибрано: велику кнопку «Наступна дія» в картці (`OrderCard.jsx`, з функцією `advance`/`nextStatus`), колонку «Наступна дія» + KPI та фільтр «Потребують дії» в реєстрі (`Orders.jsx`), рядок «наступної дії» на канбан-картці (`Kanban.jsx`). У `lib/orders.js` спрощено `FLOW` до `{s,label}`, видалено `nextActionLabel/needsAction/nextStatus`, `proposalOverdue` тепер із константою 48 год. Статус змінюється випадайкою «Статус» у вкладці «Деталі». Підсвітка «Прострочено» (КП>48год / оплата субзамовлення) лишилась — це окрема ознака, не «наступна дія». Telegram/API-бот (`api/bot.js` має власний локальний `nextStatus`) не чіпали. `npm run build` проходить. Оновлено [[project_rewrite_v2]], `ЗАМОВЛЕННЯ_ОПИС.md`.

**🔴 Fix: задвоєння номерів замовлень (2026-08-18, міграція 031 — запустити вручну).** Усі 5 місць створення заявки (веб-форма `Orders.jsx`, копіювання `OrderCard.jsx`, Telegram-бот `api/telegram.js`, API-бот `api/bot.js`, заявка з пошти `api/order-from-email.js`) рахували номер як `count(*)+1`. Ламалось двома способами: (1) **видалення** будь-якого замовлення нижче максимуму → `count` падав → наступний номер збігався з наявним (напр. два №0039); (2) **гонка** — одночасне створення з різних джерел читало однаковий `count` → однаковий номер. **Fix:** послідовність `orders_number_seq` + атомна `next_order_number()` (`nextval` — стійка до гонок і видалень) + **унікальний індекс** `orders_order_number_uniq` (тверда гарантія) + розшивання наявних дублів у міграції (найраніше замовлення групи лишає номер, решта отримують свіжі). Спільний хелпер `nextOrderNumber(client)` (`src/lib/orderNumber.js` для браузера, `api/_lib.js` для серверлес) кличе RPC, з **фолбеком на `max+1`** (не count+1) поки міграцію 031 не запущено. Доповнює [[feedback_orders_workflow]].

**Розділ «Бюджет» видалено (2026-08-18).** Функціонал застарілий і непрацюючий — прибрано повністю з фронтенду: сторінку `src/pages/Budget.jsx` (`git rm`), lazy-import + роут `/budget` у `App.jsx`, пункт меню «Бюджет» у `Layout.jsx`. **НЕ чіпали:** таблицю `plans` в БД (лишена для майбутнього переписування за новим ТЗ) і читання `plans` у `lib/pl.js` (режими «План»/«Порівняння» в Аналітиці лишились робочими, але **без редактора** — Budget був єдиним місцем вводу планів). Новий бюджет буде реалізовано за детальним ТЗ. `npm run build` проходить.

**Fix: сума прописом округлювала копійки без переносу в гривні (2026-08-12).** У всіх PDF-документах (видаткова/рахунок/акт/договори — спільна `amountInWords` у `lib/docgen/formatUtils.js`) кругла сума `310000,00` через float приходила як `309999.9999…` → `Math.floor` давав 309999 гривень, а копійки округлялись до 100 і **не переносились** назад у гривні → «…дев'яносто дев'ять гривень 100 копійок». **Fix:** рахунок через цілі копійки з переносом — `totalKop = round(abs*100); hrn = floor(totalKop/100); kop = totalKop%100`. Перевірено на круглих і дробових сумах.

**Замовлення: AI-собівартість з рахунку постачальника (2026-08-05).** Вкладка «Товари» → кнопка **«Собівартість (AI)»**: завантаження рахунку/накладної постачальника (.docx/PDF/фото) → `extractOrderItems` (переюз — вже читав накладні) витягує позиції з цінами → зіставлення з наявними рядками замовлення (за кодом `_normSku`, тоді за назвою `_jaccard` токенна схожість ≥0.4) → проставляється `cost_price`. **Рядки рахунку без пари ІГНОРУЮТЬСЯ (не додаються)** — рахунок може покривати кілька замовлень, тож заповнюємо лише наявні позиції (виправлено 2026-08-05 після дублів у 0021: раніше зайві рядки додавались новими позиціями). Порожнє замовлення підказує спершу додати позиції. **Коректність ПДВ:** собівартість зводиться до бази ПДВ рядка (`price_includes_vat`) через `_toCostBasis` (конвертація з ПДВ↔без ПДВ), щоб маржа лишалась net-to-net. Нічого не пишеться в БД до «Зберегти». Хелпери `_normName/_normSku/_tokens/_jaccard/_toCostBasis` над `ItemsTab` у `OrderCard.jsx`. Доповнює [[feedback_pricing]].

**Розрахунок для інвестора: фікс маржі + спрощення (2026-08-05).** `investorReport.js` `compute()` змішував базу ПДВ: продаж догрослювався до з ПДВ, а закупівля лишалась без ПДВ → ПДВ з продажу потрапляв у «маржу» (замовлення 0002: 6% на сторінці → хибні 21.8% у PDF, чистий прибуток 216к замість ~61к). **Fix:** закупівля і продаж зведені до однієї бази (без ПДВ), net-to-net як маржа в картці замовлення; ПДВ — pass-through, більше не віднімається з прибутку (був подвійний облік). **Спрощення (за запитом):** прибрано великі KPI-плитки зверху та блок «Висновок для рішення»; лишилась таблиця складу (ціни без ПДВ) + підсумок (виручка з/без ПДВ, собівартість/капітал, валова маржа, податок 18%, чистий прибуток, ROI). Капітал = собівартість без ПДВ. Узгоджено з net-to-net маржею зі звіту «Рентабельність» [[feedback_cost_method]].

**Замовлення: двобічна націнка (2026-08-04).** Поле «Націнка %» (`ItemsTab`, `OrderCard.jsx`) тепер працює в обидва боки: задана **закупівля** → рахує ціну продажу (як було); задана **лише ціна реалізації** (закупівля порожня) → рахує закупівлю (`закупівля = ціна ÷ (1 + %/100)`, `costFromMarkup`). Пріоритет у закупівлі, коли задані обидві. Поле активне, коли є закупівля АБО ціна продажу. Масова націнка над таблицею теж двобічна. БД не чіпається (відсоток не зберігається). Доповнює [[feedback_pricing]].

**Замовлення: ціна продажу через % націнки (2026-07-29).** У вкладці «Товари» (`OrderCard.jsx`, `ItemsTab`) додано колонку **«Націнка %»** між «Закупівля» і «Ціна продажу»: ввід відсотка рахує ціну продажу `ціна = закупівля × (1 + %/100)`; поле двобічне — показує поточну націнку з наявних цін і оновлюється при зміні закупівлі/ціни; неактивне, поки закупівля порожня. Над таблицею — **масова націнка** («Націнка на всі позиції: [%] Застосувати») — проставляє ціну всім рядкам із заповненою закупівлею. Рахується на введених значеннях, тож коректно і в режимі «Ціни з ПДВ», і «без ПДВ» (однаковий базис). Це **markup** (від закупівлі), на відміну від колонки «Маржа» (від продажу). БД не чіпається — відсоток не зберігається, виводиться з цін.

**Fix: КП не переносив одиницю виміру (2026-07-29).** У `ProposalsTab` (`OrderCard.jsx`) одиниця в позиціях КП була захардкоджена `unit: 'шт'` (`previewProposal`), а `startNew` навіть не вибирав `unit` з `order_items` — тож усі позиції в PDF йшли як «шт» (кабель у км, труби в м показувались хибно). **Fix:** `startNew` тягне `unit` і сідить у позиції; редактор КП має поле «Од.»; `previewProposal` бере `it.unit` з фолбеком за назвою з `order_items` для старих КП (збережених без unit). Одиниця зберігається в `commercial_proposals.items` (jsonb), схему не міняли.

**UX: вкладка «Товари» замовлення — фікс верстки (2026-07-30).** Рядок був `flexWrap:'wrap'` з 11 колонками → кнопка × переносилась на окремий рядок, поля розповзались. Тепер заголовок+рядки в скрол-контейнері (`overflowX:auto`) зі спільним `minWidth:1264`, рядки `flexWrap:'nowrap'` з роздільниками; × закріплено в кінці (`flexShrink:0`). Широкий екран — назва розтягується; вузький — горизонтальний скрол, колонки вирівняні. **Повний показ назви/коду:** `ProductSelect` та поле «Код» тепер — авто-зростаючий `AutoTextarea` (`components/ui/AutoTextarea.jsx`): текст переноситься на новий рядок і показується повністю, менший шрифт (12.5px), без обрізання.

**Аналітика: блок «Очікуваний результат» у P&L (2026-07-29).** P&L касовий (лише `is_validated`) → витрати на майбутні угоди вже в мінусі, а виручка ще попереду, тож поточний результат виглядає заниженим. Додано окремий блок (`computeForecast` у `pl.js`, `ForecastCard` у `Analytics.jsx`, вкладка P&L) — **фактичний P&L не змішується з прогнозом**: показує `Фактичний Net + Дебіторка (виписані видаткові/акти, ще не оплачені, з `computeAging`) + очікувана маржа з відкритих замовлень (net-to-net з `order_items`)`. **Без подвоєння:** відкриті замовлення, що вже мають receivable-документ, виключаються з pipeline (вони в дебіторці). Замовлення без позицій — маржа не врахована (показано лічильник). Явна помітка, що це оцінка.

**Fix: імпорт виписки не визначав рахунок за банком (2026-07-29).** Форма імпорту (`BankCash.jsx`, `ImportTab`) за замовчуванням ставила рахунок = перший у списку (ПУМБ). Парсер уже визначав банк (`_bank`: `Monobank`/`ПУМБ`, збігається з `accounts.bank_name`), але для вибору рахунку це не використовувалось → Mono-виписка (66 tx) осіла на ПУМБ. **Fix:** після парсингу авто-обирається рахунок за визначеним банком; біля селектора — зелений підтверд або червоне попередження про розбіжність «виписка ≠ обраний рахунок». **Дані виправлено:** 66 Mono-транзакцій (усі `bank_name=Monobank`, невалідовані) перенесено ПУМБ→Monobank (сума 172 281,45 — весь «баланс ПУМБ» був цим помилковим імпортом). **+ Дедуплікація імпорту:** `doImport` більше не дублює вже наявні транзакції — пропускає за референсом (унікальний у Monobank), а для виписок без референсу — за композитним ключем (дата+сума+контрагент+опис) у межах рахунку/діапазону дат, а також дублі всередині пачки. Після імпорту — підсумок «додано N / пропущено дублів M». **Дані:** видалено 31 дубль (повторний імпорт Mono-виписки — 66 рядків, з них 31 уже були в системі, 35 нових).

**🔴 Fix: `resolveProduct` зліплював різні товари за брендом-артикулом (2026-07-29).** AI-імпорт специфікації проставляв бренд («Мега-Кабель») у поле `sku` багатьом різним кабелям/проводам; `resolveProduct` крок 0 (збіг за артикулом) матчив усіх в один товар, додаючи кожну назву синонімом. Приклад: 14 різних позицій ВВГнг-LS/ПВС/ПВ ГУНП злиплися в один товар, склад −2.1 км замість −0.9. **Fix (`stockService.js`):** збіг за артикулом лише якщо артикул схожий на реальний код (`/\d/` — містить цифру); бренд-слово без цифри більше не використовується для ідентифікації (падає на точний синонім/назву або створює новий товар). **Дані розібрано** (скрипт, за специфікацією x/х/× уніфіковано, бренд-суфікс об'єднано): 11 окремих товарів, 3 рухи видаткової ВН-0009 перенесено на правильні товари, 14 позицій замовлення репойнтнуто, бренд-`sku` очищено. `price` в OUT-рухах — ціна продажу (не закупівля), тож `buy_price` новим не проставляли. Клас «злипань» через нечіткий/бренд-матч — див. також [[feedback_stock_integrity]].

**Документи: назва велика, номер малий (2026-07-29).** У шапці всіх PDF-документів назва типу була дрібним лейблом (7.5px), а `№` — величезним (22–24px) — виглядало незбалансовано. Тепер **назва документа — 19px жирна чорна**, а `№ … · від дата` — компактним сірим рядком (10.5px). Застосовано в шаблонах `invoice`/`waybill`/`serviceAct`/`commercialProposal`/`supplierOrder`. Договори (`supplyAgreement`/`loanAgreement`) не чіпали — там назва вже більша (центрований юридичний заголовок). Лише PDF-шаблони.

**Замовлення: поле «Договір» + фікс договору у звіті (2026-08-31, міграція 040 — запустити вручну).** У звіті по замовленнях колонка «Договір» показувала **один і той самий** договір усім замовленням клієнта — бо зв'язку «замовлення→договір» не існувало, і фолбек брав «останній» договір контрагента (напр. усім ГУНП підставлявся №1556, хоча в реєстрі 12 договорів). **Фікс:** `orders.contract_id` (FK → `contractor_contracts`, міграція 040) + випадайка **«Договір»** у вкладці «Деталі» картки замовлення (список договорів клієнта, `№… від дата`; скидається при зміні клієнта; ґрейсфул до 040). Звіт (`OrderReport`) тепер бере договір із `order.contract_id` (пріоритет — прив'язаний документ-договір isContract), **оманливий фолбек прибрано** — без прив'язки колонка порожня. Наявні замовлення: проставити договір у картці кожного (авто-мапінгу немає — договори не збігаються з замовленнями за номером/датою).

**Замовлення: звіт по замовленнях (2026-08-31).** У розділі «Замовлення» третій режим перегляду **«Звіт»** (кнопка з іконкою поряд із Таблиця/Канбан, `components/OrderReport.jsx`). Фільтри: **Замовник** (усі / конкретний контрагент), **Статус** (усі / один зі STATUS_ORDER), **Період** (з/по за датою замовлення `created_at`). Кнопка «Сформувати» → таблиця: № замовлення · Дата · Замовник · **Договір** (№+дата) · **Видаткова накладна** (№+дата) · **Рахунок** (№+дата) · Сума; футер — **Загальна сума** + лічильник замовлень. Джерела номерів/дат: прив'язані до замовлення документи (`documents.order_id`) за типом — договір=`supplyAgreement`/`loanAgreement` (фолбек — реєстр `contractor_contracts` клієнта), видаткова=`waybill`, рахунок=`invoice` (беремо найсвіжіший за `doc_date`). **Експорт в Excel** (xlsx ліниво через `import('xlsx')`, окремий чанк). Архівні замовлення виключені. **Скоупу за компанією ще нема** (до Фази 1 мультикомпанійності показує всі).

**Контрагенти: код дилера + вибір дилера у формі реєстрації (2026-09-04, міграція 048).** `contractors.dealer_code` (наш дилерський код у постачальника). У картці контрагента-постачальника (`ContractorCard DetailsTab`) — блок «Дилер/дистриб'ютор» з полем «Код компанії у дилера» (лише коли `is_supplier`; у read-only показується теж). У вкладці «Реєстрація у вендора» (`VendorRegTab`) — випадайка **«Обрати дилера»** (постачальники з `is_supplier`, показує назву + код): вибір підтягує назву дистриб'ютора (Canon D15) і **код дилера (Canon D3)** автоматично в поля форми (editable). Код дилера тепер живе на контрагенті (не лише localStorage). Код ґрейсфул до 048 (save-fallback у ContractorCard). Доповнює запис нижче.

**Замовлення: вкладка «Реєстрація у вендора» (2026-09-04).** Нова вкладка в картці замовлення (`components/VendorRegTab.jsx`) — генерує форму проектної реєстрації у дистриб'ютора/вендора **у форматі самого вендора** (заповнює оригінальний .xlsx-шаблон, зберігаючи стилі/рамки/об'єднання/інструкції). Реалізовано через **exceljs** (lazy-чанк ~940КБ, вантажиться лише при генерації; звичайний `xlsx` не пише стилі). Реєстр вендорів — `lib/vendorForms.js` (`VENDORS` + `fillVendorForm`): додати вендора = покласти шаблон у `public/vendor-forms/` + конфіг (клітинки полів + рядки устаткування). **Canon** (`public/vendor-forms/canon.xlsx`): D2 компанія · D3 код дилера (ручний, памʼятається на компанію в localStorage) · D4/D6/D7/D8 замовник (назва/контакт/ЄДРПОУ/відповідальний, авто з contractors+contractor_contacts) · рядки 10–13 устаткування (модель D / к-сть F / ціна G з order_items, до 4 рядків) · D14/D15/D17 термін/дистриб'ютор/період · D16/D18 авторизація/коментарі. Авто-поля префіляться з замовлення (компанія за company_id, клієнт за client_id, дистриб'ютор з supplier_orders), ручні редагуються. **Позиції різних вендорів в одному замовленні:** секція устаткування — чекліст усіх товарів замовлення; авто-підбір позначає ті, що містять назву вендора в назві/артикулі (перевіряється вручну); у форму йдуть лише обрані (перші maxRows, решта — попередження). Кнопка «Сформувати» віддає заповнений .xlsx. Нова залежність `exceljs`.

**Інвестору: відмітка «агентські сплачені» (2026-09-03).** У звіті «Інвестору» (`OwnerReport`) додано колонку **«Сплачено»** — чекбокс на кожен рядок (переюз `orders.commission_paid`, міграція 045; редагується інлайн, `setPaid`). Підсумки груп/футер показують лічильник «N/M сплачено». Новий KPI **«Агентські несплачені»** (сума агентських по рядках без відмітки, червоний) — власнику одразу видно борг по агентських. Excel-експорт має колонку «Агентські сплачені» (так/ні). Міграції не треба (colspan 10→11, minWidth 1060).

**Замовлення: «Виконані»/«Програні» замість архіву (2026-09-03).** Переосмислено воркфлоу завершення. Реєстр (`Orders.jsx`): фільтри тепер **Всі · Мої · Прострочено · Виконані (зелена) · Програні (червона)**; прибрано `Торгівля/Послуги/Агент` і `Архів`. Логіка: `done = !!archived_at` (під капотом лишається `archived_at`, нової міграції не треба); **Виконані** = `done && outcome≠lost` (виграні + завершені-без-результату), **Програні** = `outcome=lost`; інші фільтри показують лише в роботі. Кнопки-фільтри «Виконані»/«Програні» кольорові. Картка (`OrderCard`): замість меню «Архівувати→Виграно/Програно/Без результату» — дві кнопки **«Виконано»** (зелена, `archive('won')`) / **«Програно»** (`archive('lost')`); банер завершеного — зелений/червоний з «Виконано/Програно» + перемикач + **«Повернути в роботу»** (`unarchive`). `OUTCOME.won.label` «Виграно»→«Виконано» (icon ti-circle-check). Наявні дані лягли самі: 16 won+7 архів-null→Виконані (23), 3 lost→Програні. Слово «архів» з UI прибрано (в БД колонка лишається як механізм). Звіт «Інвестору» вже включає завершені (фікс нижче).

**Fix: архівні замовлення не потрапляли в звіт «Інвестору» (2026-09-03).** `OwnerReport` фільтрував `.is('archived_at', null)` → виконані-й-заархівовані угоди (найреальніші) випадали зі звіту. Прибрано фільтр архіву — джерело правди тепер лише ручна відмітка `in_investor`. Архівні замовлення можна позначати діамантом через фільтр-чип «Архів» у реєстрі або в картці.

**Розділ «Інвестору» + відмітка на замовленні (2026-09-03, міграція 047).** Розрахунок винесено з Аналітики в **окремий яскравий розділ навігації «Інвестору»** (`pages/Investor.jsx` → `OwnerReport`, маршрут `/investor`, іконка ti-diamond фіолет `#7C3AED`, у `permissions.js` — лише **admin+accountant**; пункт меню з `accent` підсвічений). Вкладку «Розрахунок» з Analytics прибрано. **`orders.in_investor`** (bool default **false**, міграція 047, частковий індекс): у розрахунок потрапляють **лише позначені вручну** замовлення (спекулятивні/нереалізовані не завищують цифри власнику). Відмітка — **яскравий фіолетовий діамант** (`ti-diamond-filled`) у реєстрі замовлень (окрема сортована колонка «Інвестор», тумблер без переходу в картку) + яскравий чекбокс-блок «Розрахунок інвестора» у картці «Деталі». `OwnerReport` фільтрує `.eq('in_investor', true)` (ґрейсфул: якщо колонки нема — підказка запустити 047). Доповнює запис нижче про сам звіт.

**Аналітика: вкладка «Розрахунок» для власника (2026-09-03, міграція 046).** Нова вкладка в Аналітиці (`components/OwnerReport.jsx`) за Excel-шаблоном власника — по-замовленнєвий прибуток + агентські, **ПО ВСІХ КОМПАНІЯХ** (крос-компанійний, навмисно БЕЗ scope — прямі `supabase.from`, не `qc`). Групування **по клієнту** + колонка **«Компанія»** (юрособа). Колонки: Замовлення · Компанія · Дата оплати · Закупка(net) · Реалізація(net) · ПДВ · Податок · Чистий · %агент. · Агентські; підсумки на клієнта + «ВСЬОГО» + KPI. **Формули (звірено з Excel):** маржа=реалізація−закупка (net з order_items); ПДВ=маржа×20%, податок=маржа×18% **лише для платника ПДВ** (ФОП/неплатник=0, за `orders.company_id`→`is_vat_payer`); чистий=маржа−податок (ПДВ passthrough, не віднімається); агентські=чистий×%. Ставки редаговані в шапці. **`orders.agent_commission_pct`** (міграція 046) — % на замовлення (0.1/0.15), інлайн у звіті + картці «Деталі». **Дата оплати** — найраніша вхідна банк-tx через documents→transaction_documents→bank_transactions; період: оплачені за датою оплати, неоплачені за created_at (+чекбокс «Показувати неоплачені»). Excel-експорт. Порожні замовлення пропускаються. Каверат: закупка/реалізація з order_items — точність від заповнення собівартості. Код ґрейсфул до 046.

**КП: тема документів на компанію + новий дизайн «BIT Group» (2026-09-01, міграція 044).** Додано вибір **дизайну КП на юрособу**. Міграція 044: `companies.doc_theme` (null=авто, `clean`, `bit`, `aim`). `getCompany.mapRow.docTheme`; `docOptions` → `brand.theme = company.docTheme || (isAim?'aim':'clean')`. Налаштування → «Реквізити» → випадайка **«Дизайн документів»** (Авто / Класичний лист / BIT Group (індиго) / AiM). Тепер 3 теми КП (`commercialProposal.js`): **aim** (ЕЙМ, зелений, лого вгорі), **clean** (лист ФОП, реквізити внизу синім), **bit** (новий: індиго-банер із назвою білим + лого, картка документа праворуч, таблиця з індиго-шапкою, індиго-підсумок, реквізити у футері з індиго-лінією). Спільні `productsTable`/`totalsBlock` для aim/clean; bit має власні `bitTable`/`bitTotals` (індиго). Код ґрейсфул до 044 (Settings-save fallback). Інші документи (рахунок/накладна) при theme='bit'/'clean' → clean-футер (не aim).

**КП: окремий макет для чистої теми за референсом ФОП (2026-08-31).** Для не-AiM компаній КП тепер має **іншу структуру** (`commercialProposal.js`, гілка `if (!aim)`): отримувач **«Адресовано:»** вгорі справа → заголовок «КОМЕРЦІЙНА ПРОПОЗИЦІЯ №… від …» по центру → вступ «Відповідно до Вашого запиту пропонуємо наступні товари:» → таблиця → підсумки → підпис (лінія + ПІБ + М.П./печатка) → **реквізити продавця внизу СИНІМ** (`#1560BD`, у футері сторінки): «Фізична особа-підприємець» + ПІБ (для ФОП) / назва (для ТОВ), Адреса, ІПН(РНОКПП)/ЄДРПОУ, р/р, банк; **лого — внизу зліва**. Таблиця/підсумки винесені в спільні `productsTable`/`totalsBlock` (обидві теми). AiM-тема (ЕЙМ СКІЛ) — без змін (шапка з лого/назвою вгорі). Дані продавця з активної компанії (`company.isFop`/`director`/`ipn`/`iban`/`bankName`). Синій акцент поки хардкод (`#1560BD`) — за потреби винести в поле компанії.

**Документи неплатника: усунено суперечливість ПДВ + прибрано блок-заклик КП (2026-08-31).** Рахунок/видаткова/акт/КП для неплатника (vatPayer=false) ховали колонки ПДВ, але математику лишали з ПДВ → **Ціна (net) × К-сть ≠ Сума (gross)**, і текст «у тому числі … ПДВ» / «В т.ч. ПДВ 20%» лишався (бо позиції несуть `vat_rate 20`). Фікс у `invoice`/`waybill`/`serviceAct`/`commercialProposal`: для неплатника **ціна = валова** (`r.t/r.q` замість net `r.p`) → Ціна×К-сть = Сума = Всього (24200), внутрішньо консистентно; VAT-пропис і ПДВ у призначенні платежу гейтяться на `vatPayer && vatAmount>0` (для неплатника — «Без ПДВ»). **Блок-заклик КП** («Готові обговорити деталі… Зв'яжіться з нами…») **видалено повністю** (з обох тем) за вимогою. Доповнює per-company брендинг нижче.

**КП: адаптація дизайну для не-AiM компаній (2026-08-31).** У КП для чистої теми (не ЕЙМ СКІЛ) лишались AiM-елементи в КОНТЕНТі (не лише футер): зелений акцент-бар у заголовку, зелена плашка «Всього», зелений блок-заклик і **захардкоджений `www.aim-skill.com.ua`**. Виправлено (`commercialProposal.js`): для `brand.theme==='clean'` — монохромні акценти (`accent=DARK` замість зеленого `ACCENT`), нейтральні плашки (`totalBg`/`ctaBg` сірі), email без зеленого, **AiM-сайт прибрано** (лише для aim). Додатково: КП тепер поважає `vatPayer` — для неплатника ховає колонки «ПДВ»/«Сума ПДВ» («Ціна без ПДВ»→«Ціна») і зводить підсумок до «Всього» (як інші документи). Футер сторінки й лого вже були brand-aware. Доповнює запис нижче про per-company брендинг.

**Документи per company: власне лого + AiM-брендинг лише для ЕЙМ СКІЛ (2026-08-31, міграція 043).** Кожна юрособа може мати **власний дизайн документів**, не пов'язаний з AiM. Рішення користувача: лого завантажується в Налаштуваннях; AiM-брендинг (лого AiM + футер «Сформовано в AiM Skill» + контакти AiM + лайм-акцент КП) — **автоматично лише для ЕЙМ СКІЛ** (за ЄДРПОУ 45505924 / id …0001, `company.isAim` у `companyConfig.mapRow`). Решта юросіб: своє завантажене лого (або без лого — типографічна шапка за назвою) + власні контакти у футері, без згадок AiM. **Міграція 043:** `companies.logo_base64` (PNG dataURL для pdfmake). **`docgen/index.js docOptions()`** формує `options.brand = {theme: isAim?'aim':'clean', companyLogo, name, phone, email}` і прокидає у ВСІ шаблони (разом з `vatPayer`). **Шаблони** (`invoice`/`waybill`/`serviceAct`/`supplierOrder`/`commercialProposal`/`supplyAgreement`): футер-лого = `theme==='aim' ? LOGO_BASE64 : brand.companyLogo`; рядок «Сформовано…» і контакти — умовні (AiM або назва+контакти компанії). `loanAgreement` вже без брендингу. `investorReport` (внутрішній) не чіпано. **Налаштування → «Реквізити»:** поле «Логотип для документів» (завантажити PNG/JPG/SVG → `fileToLogoDataUrl` зменшує до 320px PNG dataURL → `companies.logo_base64`; прев'ю + прибрати). Для ЕЙМ СКІЛ поле ігнорується (друкує фірмове лого AiM). `CompanyProvider` тягне лаконічний набір колонок (без важкого logo_base64). Код ґрейсфул до 043 (крім збереження лого). getCompany кешує лого й скидається при перемиканні/редагуванні.

**Замовлення: вибір/зміна компанії в «Деталях» (2026-08-31).** У вкладці «Деталі» картки замовлення додано селектор **«Компанія (юрособа)»** (показується коли компаній >1, `useCompany().companies`). Дозволяє перенести помилково заведене замовлення в іншу юрособу. При збереженні зі зміною компанії `DetailsTab.moveCompany` оновлює `company_id` самого замовлення І пов'язаних scoped-записів по `order_id` (commercial_proposals, supplier_orders, documents, generated_docs, stock_movements — best-effort у try/catch, бо док/рухи в закритому періоді можуть блокуватись тригером), потім **перемикає активну компанію** на нову (`setActiveCompany`) → Layout-remount показує замовлення вже в новій компанії (інакше воно зникло б зі scoped-вибірки). `order_items` company_id не має (йде за батьком по order_id). Міграції не треба (company_id з 041).

**Замовлення: стадії «Тендер» + «Потребує договір» (2026-08-31).** Додано два яскраві статуси: **`tender`** («Тендер», рожевий `#EC4899`) після «Новий», і **`needs_contract`** («Потребує договір», фіолетовий `#8B5CF6`) між «В опрацюванні» і «Замовлення у дистрибютора». Тепер **9 статусів**: new · tender · processing · needs_contract · ordering_supplier · shipped · needs_payment · paid · closed. Кольори всіх «яскравих» різні: тендер рожевий, договір фіолетовий, оплата помаранчева `#F97316`. `orders.status` — вільний текст, міграції не треба. Підсвітка авто через `statusAccent` (степер картки, бейдж у реєстрі, колонка канбану). Синхронізовано боти (`api/bot.js` STAGES+STATUS_LABEL, `api/telegram.js` STATUS_LABEL).

**Fix: дані не оновлювались одразу при перемиканні компанії (2026-08-31).** Сторінки вантажать дані в `useEffect(…, [])` (лише при монтуванні), а перемикач змінював тільки синглтон `activeCompanyId` → показувались старі дані до переходу між розділами. Фікс: `Layout.jsx` перемонтовує поточну сторінку при зміні активної компанії — `<div className="page-inner" key={activeId}>` (useCompany). Плюс `CompanyProvider.applyActive` скидає `resetClassifyCache()` (авто-класифікація будується зі scoped-транзакцій; getCompany-кеш уже скидався). Контрагенти/статті — спільні, кеші не чіпаємо.

**Неплатники ПДВ: функціонал «без ПДВ» (2026-08-31).** Для компаній з `is_vat_payer=false` (ФОП 2 гр., ТОВ 3 гр. 5%) увесь ПДВ-функціонал вимикається автоматично за активною компанією (`useCompany().active.is_vat_payer`; `vatOn = is_vat_payer !== false`). Міграції не треба. **Товари замовлення** (`OrderCard ItemsTab`): нові/додані позиції отримують `vat_rate=0` (defVat), колонки «Тип ціни» і поле «ПДВ», рядок «Без ПДВ» та метрики «Виручка без ПДВ»/«ПДВ» приховані, підсумок «Всього з ПДВ»→«Всього» (GRID без колонки «Тип ціни»). **Генерація** (`DocGenModal`): дефолт `vatRate=0`, приховано перемикач ціни net/gross, колонку «ПДВ%», підсумки зводяться до «Всього (без ПДВ)». **PDF-шаблони** (`invoice`/`waybill`/`serviceAct`): `docgen/index.js` прокидає `options.vatPayer = company.isVatPayer` у всі виклики шаблонів; за `vatPayer=false` таблиця без колонок «ПДВ»/«Сума ПДВ» («Ціна без ПДВ»→«Ціна»), у підсумках лише «Всього». КП — per-item (позиції з vat 0 → без ПДВ, окремо не чіпали). Математика й так колапсує в net=gross при vat 0; це прибирає ПДВ-контроли/колонки з UI та друку.

**Мультикомпанійність — Фаза 1 інкремент 5 + ЗАВЕРШЕННЯ (2026-08-31).** docgen/OCR per company + редактор компаній. Міграції не треба (таблиця `companies` з 039). **`getCompany()`** (`companyConfig.js`) тепер повертає реквізити **АКТИВНОЇ** компанії з таблиці `companies` (мапінг snake→camel, кеш за id, фолбек на стару конфігурацію якщо активної нема); `clearCompanyCache()` скидає кеш; `CompanyProvider.applyActive` скидає його при перемиканні → усі генеровані документи друкують реквізити активної юрособи. **Редактор компаній** (Налаштування → «Реквізити», `CompanyTab`): редагує рядок активної компанії в `companies` (усі реквізити + `tax_group`/`is_vat_payer`/`is_fop`); зберігає → `clearCompanyCache()` + `CompanyProvider.reload()` (перезавантажує список зі збереженням активної). Щоб редагувати іншу — перемкнути в шапці. **OCR-напрям** (`ai.js extractDocumentMulti`): «Наша компанія» у промпті будується з активної (`shortName`/`edrpou` або `ipn`) — раніше хардкод «ЕЙМ СКІЛ 45505924». **Банк-імпорт** (`BankCash ImportTab`): пропуск своїх контрагентів — набір ЄДРПОУ/ІПН **усіх** юросіб з `companies` (fallback `45505924`), раніше хардкод одного. **Шаблони ФОП:** `rvLine` уже пропускає порожні → ФОП без ЄДРПОУ показує лише ІПН; підпис бере `director_position` (можна «ФОП»). ПДВ у документах рахується з `vat_rate` позицій (не з `is_vat_payer`) — для неплатника ставити 0% у позиціях. **✅ Фаза 1 мультикомпанійності завершена** (інкр. 1-5): гроші, документи, замовлення, склад, docgen/OCR — усе за активною компанією. Далі (за потреби): RLS-замок по company_id, per-company нумерація замовлень, консолідація (level B) — не заплановано.

**Мультикомпанійність — Фаза 1 інкремент 4 (2026-08-31, міграція 042 LIVE).** Скоуплено склад: `stock_movements` + `assemblies` + в'юха `product_stock` за компанією. Міграція 042 переробила `product_stock` (DROP+CREATE, бо `select p.*` + нові колонки products → REPLACE впав би) — тепер групує залишок за `(product_id, company_id)` і виводить `company_id` (в кінці), + `grant select ... to anon, authenticated`. Код: читання складу через `qc()`, усі insert рухів/збірок (createStockMovement, збірка+реверс, ручний рух/коригування/списання у Warehouse, рухи з docgen) через `withCompany()`. `product_stock` додано у `SCOPED_TABLES`; усі його читання — `.select()` одразу після `qc()` (щоб проксі скоупив і відсікав дублі рядків на товар). **`getAllBalances`** (баланси у списку контрагентів) переписано з в'юхи `contractor_balances` на прямі scoped-запити documents+bank_transactions → в'юха `contractor_balances` більше не використовується (лишена в БД без змін). `getContractorBalance` (картка) вже був scoped. **Наслідок:** товар без жодного руху в активній компанії (`company_id=NULL` у в'юсі, ~297 таких) НЕ показується в її складі — нова юрособа стартує з порожнім складом; для ЕЙМ СКІЛ Склад показує 269 товарів з рухами замість ~566 (0-залишкові каталожні позиції сховані). **⚠ Код вимагає 042 ДО деплою** (product_stock company_id). Лишилось у Фазі 1: docgen/OCR per company (інкр.5).

**Мультикомпанійність — Фаза 1 інкремент 3 (2026-08-31).** Скоуплено `orders` + `commercial_proposals` + `supplier_orders` за активною компанією: реєстр Замовлень (`Orders.jsx` — список/KPI/канбан/звіт), картка (`OrderCard` — копіювання, КП, субзамовлення), звіт замовлень (`OrderReport`), P&L, картка контрагента. Читання через `qc()`, insert (нове замовлення, копія, чернетка КП, ручне/авто субзамовлення) через `withCompany()`. Дочірні `order_items`/`supplier_order_items`/`commercial_proposals.items` — через батька (без власного company_id). **Нумерація замовлень лишається ГЛОБАЛЬНОЮ** (`next_order_number()` + унікальний індекс на order_number — спільні для всіх компаній; per-company нумерація = окрема задача, потребує composite unique). Міграції не треба (стовпці з 041). Лишилось у Фазі 1: склад+в'юхи (інкр.4), docgen/OCR per company (інкр.5).

**Мультикомпанійність — Фаза 1 інкремент 2 (2026-08-31).** Скоуплено `documents` + `generated_docs` за активною компанією: реєстр Документів, картка контрагента, борги/дебіторка (`debts.js`), P&L/ПДВ/рентабельність (`pl.js`), закриття періоду, генерація (`docgen/index.js` — і `generated_docs`, і дзеркало в `documents`), звіт замовлень (`OrderReport`). Усі читання через `qc()`, усі insert через `withCompany()`. **🔴 Фікс критичної колізії:** хелпер перейменовано `q()` → **`qc()`** (query-company), бо `q` часто локальна змінна (пошук `const [q,setQ]` у BankCash, лічильники в pl.js/periodClose) — інкремент 1 через це ламав вкладку «Транзакції» на проді (`q` — рядок, викликався як функція). Тепер `qc` унікальне, колізій немає (звірено: 0 локальних `qc`). Правило скоупингу лишилось: `qc(table).select(...)` авто-додає `.eq('company_id')` **лише коли `.select()` — перший виклик** (insert/update/delete проходять; при неправильному порядку — просто не скоупить, не падає).

**Мультикомпанійність — Фаза 1 інкремент 1 (2026-08-31, міграція 041 LIVE).** Реальний скоуп даних за компанією почато. Міграція 041 додала `company_id` (default ЕЙМ СКІЛ `…0001`, NOT NULL, FK, index) на **всі 14 грошових таблиць** (accounts, bank_transactions, documents, generated_docs, orders, commercial_proposals, supplier_orders, stock_movements, assemblies, period_closings, tasks, emails, plans, notes) — схема готова для наступних інкрементів; наявні рядки backfill на `…0001` (звірено: 0 сторонніх). Хелпери в `lib/companyScope.js`: **`q(table)`** — проксі-скоупер, що авто-додає `.eq('company_id', активна)` одразу після `.select()` (insert/update/delete проходять без змін); **`withCompany(payload)`** — тег company_id на insert (об'єкт/масив). Коли `activeCompanyId` null (до завантаження CompanyProvider) — q() не фільтрує (безпечний фолбек). **Цей інкремент скоупить лише `accounts` + `bank_transactions`:** усі читання (Банк/Каса `BankCash`, баланси `accounts.js`, P&L `pl.js`, aging `debts.js`, авто-класифікація `autoClassify.js`, картка контрагента, закриття періоду `periodClose.js`/`PeriodClose.jsx`) через `q()`, усі insert (додати операцію, імпорт виписки, переказ) через `withCompany()`. `contractors.js` НЕ чіпано (будує спільний довідник, бере supabase як параметр). **⚠ Код вимагає 041 ДО деплою** (інакше читання банку падають на відсутній стовпець). Наступні інкременти: documents+generated_docs, orders+КП+субзамовлення, склад+в'юхи, docgen/OCR per company.

**Мультикомпанійність — Фаза 0 (2026-08-31, міграція 039 — запустити вручну).** Старт великої задачі «вести кілька юросіб з перемикачем» (рівень A — окремі книги + перемикач, БЕЗ консолідації; рішення в [[project_intercompany]]). Прийняті рішення користувача: **одна база + `company_id`** (не окремі Supabase-проєкти); **контрагенти й прайси СПІЛЬНІ**, решта (гроші/склад/документи/замовлення/борги/P&L) — окремі per company; **компанії призначаються користувачам** (`user_companies`). Фаза 0 (готово, код ґрейсфул до міграції): таблиці `companies` (реквізити + `tax_group`/`is_vat_payer`/`is_fop` — для ФОП/без-ПДВ шаблонів) і `user_companies`; сід 3 юросіб (ЕЙМ СКІЛ id `…0001` + плейсхолдери ФОП 2гр `…0002` / ТОВ 3гр 5% `…0003`, реквізити заповнити у Фазі 5). Код: `lib/companyScope.js` (module-singleton активного `company_id` + `SCOPED_TABLES`), `lib/company.jsx` (`CompanyProvider`/`useCompany`/`CompanySwitcher`, фолбек «усі компанії» коли призначень нема), перемикач у сайдбарі (десктоп+мобільний) і мобільній шапці, `App.jsx` обгорнуто в `CompanyProvider`. **Скоупингу запитів ще НЕМА** — це Фаза 1 (колонки `company_id` default=`…0001`+backfill, потім `q()/insertScoped` фільтрація). Наступні фази: 1 гроші+документи · 2 замовлення/КП/пошта · 3 склад+в'юхи `product_stock`/`contractor_balances` · 4 docgen/OCR per company · 5 CRUD компаній + прив'язка користувачів. Ризик: RLS відкритий → ізоляція на рівні застосунку (пропущений фільтр = чужі дані в звіті).

---

## 5. Відкриті задачі (checklist)

### 🔴 Міграції (звірено з БД 2026-07-14 через `migrations/probe.mjs`)
- [x] ~~Запустити `migrations/025_suborder_item_ordered.sql`~~ — застосовано (звірено 2026-07-14, `supplier_order_items.ordered` є).
- [x] ~~Запустити `migrations/027_generated_docs_refs.sql`~~ — застосовано (звірено 2026-07-14, усі 4 колонки є; поля видаткової/акту тепер зберігаються). Усі міграції 001–027 застосовані.
- [x] ~~028/029/030~~ — застосовано (звірено 2026-08-18 через schema-probe: `orders.outcome`, `documents.is_verified`, таблиця `tasks` існують).
- [x] ~~Запустити `migrations/031_order_number_unique.sql`~~ — застосовано (звірено 2026-08-18: `next_order_number()` існує, дублів нема, послідовність на 45 → наступне замовлення 0046).
- [x] ~~Запустити `migrations/032_orders_status_simplify.sql`~~ — застосовано (2026-08-18). Наявні замовлення переназначено на новий набір з 6 статусів.
- [x] ~~Міграції 033–037~~ — застосовано (2026-08-25): procurement_id, total-gross backfill, profiles-admin RLS, handle_new_user fix, orders.manager_id. **Усі міграції 001–037 застосовані.**

### Дії користувача (дані, не код)
- [ ] Ввести **початковий залишок ПУМБ** станом на 2025-02-26 (Налаштування → Рахунки) — інакше баланс = лише рух.
- [ ] Класифікувати **32 невалідовані транзакції** (Банк/Каса → Непідтверджені) — для точності P&L (на баланс не впливає).
- [x] ~~Розпізнати мігровані документи~~ — масове розпізнавання виконано (143 OK, 154/157 з типом+сумою). Тип береться з OCR docType (`typeFromOcr`). Лишилось: 3 завеликі файли (>~4.5МБ — ліміт Vercel-проксі) + ~10 з незнайденим контрагентом (переважно «Управління ООН…»).
- [ ] Протестувати на реальних файлах: імпорт виписки ПУМБ/Mono, генерацію PDF.

### Пошта (LIVE; переархітектовано на розділ «Пошта» 2026-06-24)
- [x] ~~Міграція 006~~ — застосовано (`mail_log`, `documents.source`; `processed_emails` більше не використовується).
- [x] ~~Env у Vercel~~ — `MAIL_USER`=office@aim-skill.com, `MAIL_PASS`, `SUPABASE_SERVICE_KEY` (`sb_secret_…`), `CRON_SECRET`.
- [x] ~~Запустити `migrations/007_mail_section.sql`~~ — застосовано (звірено 2026-07-14, таблиця `emails`, 152 рядки).
- [ ] Після деплою: відкрити **Пошта → «Синхронізувати»** (підтягне 10 днів; великий обсяг — натиснути кілька разів, поки `newEmails`=0).
- [ ] Перевірити **«Створити заявку»** на листі (AI → замовлення + документи).
- [ ] Перевірити **надсилання** (кнопка «Надіслати email» у документі) — має зʼявитись і в «Пошті» (вихідний).
- [ ] (Опц.) Зовнішній пінгувач (cron-job.org) на `POST /api/mail-sync`, заголовок `Authorization: Bearer <CRON_SECRET>`.

### Технічні (код/інфра)
- [ ] Перевірити, що **політика Storage** дозволяє authenticated download (інакше «Розпізнати» дасть помилку доступу до файлу).
- [ ] Розглянути **ротацію Supabase service-role ключа** (був у локальній git-історії, на GitHub не потрапив).
- [ ] Видалити legacy-таблицю `cash_transactions` після перевірки (нова апка її не читає; 1 рядок уже мігровано в Готівку).
- [ ] (Опц.) Рішення про конвертацію `projects` (22) → `orders` (відкладено з Фази 4).
- [ ] (Опц.) Перемикач **accrual-COGS** (зараз P&L касовий).
- [ ] (Опц.) Імпорт реальної виписки Monobank (зараз усі транзакції на ПУМБ).

---

## 6. Архітектурні рішення (НЕ переробляти)

1. **Additive-еволюція БД на місці** — той самий Supabase-проєкт, без паралельної схеми.
2. **bank_transactions = єдине джерело правди.** Борги = документи − транзакції, авто, ніколи вручну.
3. **Каса = рахунок** (`accounts.type='cash'`), не окремий модуль/таблиця.
4. **Баланс рахує ВСІ неігноровані транзакції** (валідація — лише для P&L, не для «чи рухнулись гроші»). Залишок = `opening_balance` + рухи.
5. **`bank_transactions.direction`** лишається у P&L-семантиці (Доходи/Витрати/ПФД/Інше); in/out обчислюється похідно. (Стара логіка від цього залежала — не міняти на in/out.)
6. **tx↔doc many-to-many через `transaction_documents`** (без таблиці order_transactions). Ланцюг tx → документ → замовлення.
7. **P&L касовий** (закупівлі сидять у `pl_level='cogs'` — без подвійного обліку). FIFO `cost_price` на OUT-рухах доступний для майбутнього accrual.
8. **Переюз робочих lib** зі старої системи (autoClassify, ai, docgen, stockService, vkursi, contractors) — не переписувати з нуля.
9. **Сторінки lazy-load** в App.jsx — важкий `pdfmake` ізольований у чанку Документів.
10. **`dist/` у .gitignore** — Vercel будує з джерела.
11. **Жодних секретів у репо** — `validate.mjs` читає `SUPABASE_SERVICE_KEY` з env. GitHub push-protection увімкнено.
12. **Ролі** (admin/accountant/manager/viewer) — **розмежування розділів на рівні UI** (2026-08-25, `lib/permissions.js`): сайдбар/роути фільтруються за роллю. RLS у БД поки лишається відкритим авторизованим (справжній замок — окрема фаза). Матрицю доступу міняти в `permissions.js`.
13. **`getAccountBalances()` (lib/accounts.js)** — єдиний розрахунок залишків для Банк/Каса, Дашборду, Бюджету.
15. **Рахунок (invoice) не створює борг** — борг лише з реалізованих документів (накладна/акт), щоб рахунок+накладна не задвоювали. `NON_DEBT_TYPES` у lib/debts.js.
14. **`callClaude()` (lib/ai.js)** — єдиний виклик Claude з авто-повтором; OCR і виписки через нього.
18. **Закриття періоду (розділ «Закриття періоду», `/period-close`, міграція 026).** `period_closings` (рік/місяць/status closed|reopened/snapshot jsonb). Закриття = чек-лист якості (мінусові залишки, док без сум = блокери; невалідовані тр = попередження) → знімок (P&L totals, маржа grand, склад qty+оцінка станом на кінець міс, баланси рахунків, дебітор/кредитор) → upsert. **Жорстке блокування — тригери БД** `guard_period_by_date` (stock_movements, bank_transactions) і `guard_period_by_docdate` (documents): будь-який insert/update/delete з датою в закритому місяці → `raise exception PERIOD_CLOSED` (діє навіть на service-role скрипти). Переоткриття (admin) знімає блок. `lib/periodClose.js`. Щоб виправляти закритий місяць — спершу «Переоткрити».
16. **Собівартість у звіті «Рентабельність» = ЗА ОСТАННЬОЮ ЗАКУПІВЛЕЮ** (не FIFO, не середньозважена — вибір користувача 2026-07-02). `cost_price` на OUT-рухах = ціна найсвіжішої прихідної (з документом) того товару; збігається з колонкою «Джерело закупівлі» (`purchaseByProd` у `salesProfitReport`, теж бере останню прихідну). НЕ відкочувати на середньозважену. Перерахунок — скриптом (остання IN-ціна на товар → `products.buy_price` + усі OUT `cost_price`).
17. **Ціни рухів складу — у NET (без ПДВ), єдина база.** OCR інколи читав ціни gross. Знормалізовано 2026-06/07 (÷1.2 де Σ рядків ≈ сумі документа з ПДВ). Звіт множить на 1.2. Колонка `stock_movements.total` = `price × quantity` (NET) — використовується для оцінки складу (`Warehouse.jsx`), тримати синхронно з `price`. Незмішувані винятки з нестандартним ratio (Гігаклауд-послуги 2.4, ПРООН №1 недобір рядка) лишені як є.

---

## 7. Відомі проблеми / баги (не виправлені)

- **Aging/борги майже порожні** — бо `documents.amount` у 159 мігрованих документів = null. Наповниться після «Розпізнати» по кожному або backfill.
- **Storage download для authenticated не перевірений** — кнопка «Розпізнати» може впасти з помилкою доступу, якщо немає select-політики на бакет `documents`.
- **OCR зрідка падає** при сильному перевантаженні Anthropic навіть після ретраїв — повторити через хвилину.
- **Рахунки розподілено:** ПУМБ (66 тр, bank_name='ПУМБ'), Monobank (410 тр, решта — основний рахунок), Готівка (1). Перепризначено за bank_name. Майбутні Mono-імпорти → рахунок обирає користувач при імпорті.
- **Готівка** — 1 мігрований рядок (271.20); початковий залишок каси не заданий.
- **Аудит аліасів (`migrations/audit_aliases.mjs`, прогін 2026-07-14) — до розбору вручну.** Шукає злипання товарів (наслідок старого нечіткого `resolveProduct`).
  - *Мінусові залишки (13):* послуги (Агентські/Консультаційні) — норма. Реальні товари з мінусом: HP 250R G9 i3-1315U (**−68**), Перехідник Vinga VCPATCLGBC (−30), ASUS Vivobook Go 15 …1236/…1195 (−22/−8), Samsung QLED 43" (−4), монітор 24E1N1100A / iPhone 13 Pro / Logitech Group / панель TE-YL7-86 / HH Tensor-GO (−2), Kingston IronKey D500S (−1). Причина по кожному — злипання АБО не введений початковий залишок (розбирати поштучно).
  - *Підозрілі аліаси — справжні злипання:* **Студійна стійка Arsenal ARS-2600** ← аліаси «Godox 90F» (інший бренд/висота); **Фото-сумка Case Logic DCB-304** ← «Canon EOS camera bag».
  - *Хибні спрацювання (той самий товар, інший опис — НЕ чіпати):* HP 250R G9 Core 3-100U ← «15FI/Core3 100U…»; Миша ESPERANZA ← «Mouse MX405 CYBORG EGM405»; Samsung QLED 43" ← «Мультимедійна панель QE43Q7FAAUXUA».
  - Рішення користувача 2026-07-14: **лишити як звіт**, не фіксити зараз.

---

## 8. Міграції (порядок)

| Файл | Призначення | Стан |
|---|---|---|
| `001_phase1_schema.sql` | Базова additive-схема | ✅ застосовано |
| `002_cutover.sql` | Каса→банк, backfill сум документів | частково (каса домігровано вручну; backfill сум документів — ні) |
| `003_account_opening_balance.sql` | Колонки початкового залишку | ✅ застосовано |
| `004_documents_update_and_docnumber.sql` | UPDATE-RLS на documents + doc_number | ✅ застосовано |
| `005_document_date.sql` | Колонка `documents.doc_date` | ✅ застосовано |
| `006_mail_integration.sql` | Пошта: `processed_emails`, `mail_log`, `documents.source` | ✅ застосовано |
| `007_mail_section.sql` | Розділ «Пошта»: таблиця `emails` | ✅ застосовано (звірено 2026-07-14, 152 рядки) |
| `008_order_archive.sql` | Архів замовлень: колонка `orders.archived_at` | ✅ застосовано |
| `009_order_items.sql` | Позиції товарів замовлення: таблиця `order_items` | ✅ застосовано |
| `010_supplier_price_lists.sql` | Прайси постачальників: `supplier_price_lists`, `supplier_prices` | ✅ застосовано |
| `011_supplier_prices_currency.sql` | Прайси: валюта/курс/ПДВ (`currency`,`price_original`,`vat_rate`,`usd_rate`) | ✅ застосовано |
| `012_supplier_prices_extra.sql` | Прайси: `uktzed`, `warranty`, `warranty_term` | ✅ застосовано (звірено 2026-07-14) |
| `013_order_items_cost.sql` | Позиції замовлення: `cost_price` (маржа) | ✅ застосовано |
| `014_suborders_auto.sql` | Авто-субзамовлення: `order_items.supplier_id`, `supplier_orders.source`, `supplier_order_items.name/unit` | ✅ застосовано |
| `015_order_items_vat.sql` | Позиції замовлення: `vat_rate` (розрахунок з/без ПДВ) | ✅ застосовано |
| `016_order_items_vat_incl.sql` | Позиції: `price_includes_vat` (ПДВ у ціні чи зверху) | ✅ застосовано |
| `017_documents_generated_link.sql` | Дзеркало згенерованих: `documents.generated_doc_id` | ✅ застосовано |
| `018_orders_procurement.sql` | Замовлення: `procurement_type` (тендер/пряма) | ✅ застосовано |
| `019_items_sku.sql` | Код товару: `order_items.sku`, `supplier_order_items.sku` | ✅ застосовано (звірено 2026-07-14) |
| `020_orders_lead_source.sql` | Бот: `orders.lead_source` + таблиця `bot_sessions` | ✅ застосовано (звірено 2026-07-14) |
| `021_proposals_storage.sql` | КП: `commercial_proposals.storage_path` (перегляд з бота) | ✅ застосовано (звірено 2026-07-14) |
| `022_pricelist_source.sql` | Прайси: `supplier_price_lists.source` ('file'\|'brain_api') для API-синку Brain | ✅ застосовано (звірено 2026-07-14) |
| `023_brain_categories.sql` | Прайси: `supplier_price_lists.categories` jsonb (обрані категорії Brain) | ✅ застосовано (звірено 2026-07-14) |
| `024_contractor_edr_extract.sql` | Контрагенти: `edr_extract_path`/`edr_extract_name` (файл витягу з ЄДР) | ✅ застосовано (звірено 2026-07-14) |
| `025_suborder_item_ordered.sql` | Субзамовлення: `supplier_order_items.ordered` (помітка «замовлено») | ✅ застосовано (звірено 2026-07-14) |
| `026_period_closings.sql` | Закриття періоду: `period_closings` + тригери-блокувачі `guard_period_by_date`/`guard_period_by_docdate` | ✅ застосовано (LIVE) |
| `027_generated_docs_refs.sql` | Генерація: `generated_docs.invoice_ref`/`invoice_ref_date`/`delivery_basis`/`delivery_address` (щоб поля видаткової/акту не «злітали» при редагуванні) | ✅ застосовано (звірено 2026-07-14) |
| `028_orders_outcome.sql` | Замовлення: `orders.outcome` ('won'\|'lost'\|null) — результат при архівуванні | ✅ застосовано (звірено 2026-07-14) |
| `029_documents_verified.sql` | Документи: `is_verified`/`verified_at`/`verified_by` — перевірка перед закриттям періоду | ✅ застосовано (звірено 2026-08-18 через schema-probe) |
| `030_tasks.sql` | Задачі: таблиця `tasks` (+ RLS authenticated) для розділу «Задачі» й нагадувань бота | ✅ застосовано (звірено 2026-08-18 через schema-probe) |
| `031_order_number_unique.sql` | Замовлення: послідовність `orders_number_seq` + `next_order_number()` + унікальний індекс + розшивання дублів (фікс задвоєння номерів) | ✅ застосовано (звірено 2026-08-18: функція існує, дублів нема, setval=45 → наступне замовлення 0046) |
| `032_orders_status_simplify.sql` | Замовлення: переназначення наявних замовлень на спрощений набір з 6 статусів | ✅ застосовано (2026-08-18, Success) |
| `033_orders_procurement_id.sql` | Замовлення: `procurement_id` (ідентифікатор тендерної закупівлі) | ✅ застосовано (звірено 2026-08-25, колонка є) |
| `034_orders_total_gross.sql` | Замовлення: backfill `orders.total` = сума з ПДВ з `order_items` | ✅ застосовано (2026-08-25) |
| `035_profiles_admin_rls.sql` | Профілі: `is_admin()` + політики (адмін бачить усіх і міняє ролі) | ✅ застосовано (звірено 2026-08-25, функція є) |
| `036_fix_handle_new_user.sql` | Фікс тригера `handle_new_user` (search_path) — «Database error creating new user» | ✅ застосовано (2026-08-25) |
| `037_orders_manager.sql` | Замовлення: `manager_id` (відповідальний) + політика `profiles_select_all` | ✅ застосовано (звірено 2026-08-25, колонка є) |
| `038_documents_posted.sql` | Документи: `posted` (чернетка в замовленні / проведено по системі) | ✅ застосовано (звірено 2026-08-31) |
| `039_companies.sql` | Мультикомпанійність Фаза 0: таблиці `companies` + `user_companies`, сід 3 юросіб (ЕЙМ СКІЛ + ФОП 2гр + ТОВ 3гр 5%), RLS, призначення всіх користувачів усім компаніям | ✅ застосовано (звірено 2026-08-31: 3 компанії, 6 призначень) |
| `040_orders_contract.sql` | Замовлення: `contract_id` (FK → contractor_contracts) — договір замовлення для звіту | ✅ застосовано (звірено 2026-08-31) |
| `041_company_scope.sql` | Мультикомпанійність Фаза 1: `company_id` (default ЕЙМ СКІЛ, NOT NULL, FK, index) на 14 грошових таблиць | ✅ застосовано (звірено 2026-08-31: усі 14, backfill на …0001, 0 сторонніх) |
| `042_product_stock_company.sql` | Мультикомпанійність Фаза 1: в'юха `product_stock` per-company (company_id, group by product+company) | ✅ застосовано (звірено 2026-08-31: company_id є, 269 рядків ЕЙМ СКІЛ) |
| `043_company_logo.sql` | Документи per company: `companies.logo_base64` (власне лого) | ✅ застосовано (звірено 2026-08-31) |
| `044_company_doc_theme.sql` | Дизайн документів на компанію: `companies.doc_theme` (clean/bit/aim) | ✅ застосовано (звірено 2026-09-03) |
| `045_orders_commission_paid.sql` | Замовлення: `commission_paid` (чекбокс «комісійні сплачені» в реєстрі) | ✅ застосовано (звірено 2026-09-03) |
| `046_orders_agent_pct.sql` | Замовлення: `agent_commission_pct` (% агентських для звіту «Розрахунок») | ✅ застосовано (звірено 2026-09-03) |
| `047_orders_investor.sql` | Замовлення: `in_investor` (відмітка «врахувати в розрахунку інвестора») + частк. індекс | ✅ застосовано (звірено 2026-09-03: 19 позначено) |
| `validate.mjs` | Перевірка цілісності Фази 1 (`SUPABASE_SERVICE_KEY=... node migrations/validate.mjs`) | — |
| `probe.mjs` | Звірка наявності колонок/таблиць усіх міграцій у БД (`SUPABASE_SERVICE_KEY=... node migrations/probe.mjs`) | — |

---

## 9. Робочий процес

- Гілка `main` → авто-деплой Vercel. Коміти українською, з `Co-Authored-By`.
- DDL — у Supabase Dashboard вручну (готую SQL, користувач запускає).
- Перед пушем: `npm run build` має пройти; перевірити відсутність секретів (`grep sb_secret_`).
- **Цей файл (CONTEXT.md) оновлювати після кожної завершеної задачі.**
